# BOARD.md — kolejka przekazań CC ↔ Codex

Nie czytane automatycznie jak AGENTS.md — trzeba wprost polecić "na początku
czytaj BOARD.md" (patrz AGENTS.md). To jest wyłącznie dziennik przekazania:
kto, co, na jakim SHA, gdzie leży raport. Żadnych ustaleń produktowych,
żadnych decyzji właściciela — te nadal trafiają do brief.md/kontraktu danego
Tasku. PR pozostaje realnym wyzwalzem pracy; ten plik tylko rejestruje
przekazanie, żeby nie trzeba było ręcznie przeklejać wiadomości między CC a
Codexem.

Statusy (dokładnie cztery, nic więcej):

- `READY_FOR_CODEX` — CC skończył, czeka na audyt.
- `CODEX_IN_PROGRESS` — Codex audytuje.
- `CODEX_REPORTED` — raport gotowy pod wskazaną ścieżką.
- `OWNER_DECISION_NEEDED` — audyt utknął na decyzji właściciela.

Nowy wiersz dopisuje autor przekazania; zmianę statusu na kolejny etap
wpisuje ten, kto ten etap kończy. Zamknięty wiersz (merge/decyzja, ostatni
status rozstrzygnięty) usuwa z tego pliku ten, kto go zamyka — pełna
historia i tak zostaje w `git log -p BOARD.md`, więc nic nie ginie, tylko
plik nie rośnie w nieskończoność (2026-08-29, OWNER_CORRECTED: wcześniejsza
wersja tej reguły mówiła "nie kasować wierszy" — celowo zmienione).

| ID | Autor | Odbiorca | Branch | Exact SHA | Status | Wiadomość |
|---|---|---|---|---|---|---|
| ROTA-NO-PERSISTENT-ERROR-LOG + ROTA-TECHNICAL-ERROR-RECOVERY-UX | CC | Codex | `task/ROTA-TECHNICAL-ERROR-RECOVERY-UX` | `05f2e2a` (impl, brief `fe689a1`) | READY_FOR_CODEX | **Implementacja gotowa do audytu.** Nowy `api/runtime_log.py`: jeden sanitizowany rotating log (`runtime-errors.log`, 1 MiB + 4 kopie); pola wyłącznie timestamp/severity/component/exception-type/safe-message/stack-trace/incident_id, nigdy display_name/treść formularzy/request-response body/payloady decyzji/sekrety. LOCAL_WINDOWS default = podkatalog `logs/` obok istniejącego `DB_PATH`; CENTRAL_SERVICE wymaga jawnego `ROTA_LOG_DIR` (fail-closed). **Decyzja implementacyjna do potwierdzenia:** CENTRAL_SERVICE wykrywane przez reuse istniejącego sygnału `pii_crypto.CENTRAL_KEK_ENV_VAR` (`ROTA_CENTRAL_KEK`), zamiast wprowadzać drugi znacznik modelu wdrożenia — brief nie precyzuje mechanizmu detekcji, tylko kontrakt (LOCAL_WINDOWS default / CENTRAL_SERVICE wymaga ROTA_LOG_DIR). `api/errors.py::to_http_exception`: gałąź nieskalsyfikowanego wyjątku (jedyna, do której to dotyczy — każdy sklasyfikowany wyjątek domenowy ma już bezpieczny wynik i nigdy tu nie loguje) zapisuje jeden wpis; publiczny komunikat zmieniony na nowe zamrożone zdanie, współdzielone z frontendem. `api/routers/schedule.py::_planning_result_out`: jeden `operation`-parametryzowany boundary dla PLAN/REPLAN/wider-search/retry; structured `TECHNICAL_ERROR` (brak exception context) daje dokładnie jeden wpis kategorii `STRUCTURED_PLANNING_FAILURE`, nigdy `PlanningResult.error_message`. `rota/application/backup.py::build_diagnostic_zip`: dołącza `runtime-errors.log*` gdy istnieją (rozwiązanie ścieżki zostaje po stronie api/routera — moduł application zostaje frameworkowo-agnostyczny); brak logu nie psuje ZIP-a. Frontend: `client.ts` eksportuje jedno zamrożone `TECHNICAL_ERROR_MESSAGE`, używane dla non-public 5xx / utraty sieci / timeoutu (non-public 4xx zachowuje swój neutralny tekst — to problem wejścia, nie techniczny). `MonthlyPlanning.tsx`: TECHNICAL_ERROR nie renderuje już surowego `error_message`; pokazuje zamrożony komunikat, przycisk pobrania istniejącego pakietu diagnostycznego i wyszarzony mockup "Kontakt ze wsparciem". Testy: `tests/test_runtime_error_log.py` (nowy, 14 przypadków: sanityzacja, rotacja A7, unikalność incident_id, rozdzielczość katalogu A9/A10, boundary A2/A6, ZIP z/bez logu A8, jeden pion integracyjny sekcji 10: realny nieklasyfikowany wyjątek -> HTTP -> log -> ZIP). Pełna regresja: 1342 passed, 1 skipped, ten sam 1 nieistotny pre-existing FAIL (`test_t023_checkpoint_b.py`, sprzed tej sesji). `ruff check`, `tsc --noEmit`, `vite build` czyste. **Znaleziony i NIE naprawiony (poza zakresem tego Tasku, zgłoszony osobno):** `frontend/e2e/diagnostics.spec.ts` test "API 500" oczekuje, że surowe body odpowiedzi (`"boom"`) jest widoczne koordynatorowi przy nieklasyfikowanym 5xx — zweryfikowane bezpośrednio: ten test failuje JUŻ na stanie sprzed tego Tasku (`fe689a1`/`40d1e95`, sprawdzone przez tymczasowe przywrócenie oryginalnego `client.ts` i uruchomienie testu), sprzed kontraktu nagłówka publicznego błędu T060. Zmiana komunikatu w tym Tasku nic w tym nie psuje ani nie naprawia — wymaga osobnej decyzji, czy ten test w ogóle jest jeszcze aktualny.

**Historia przekazania (poprzednia runda):** **R2 literalny re-check: 3/3 TAK; IMPLEMENTATION HOLD ZWOLNIONY.** `_planning_result_out` obejmuje PLAN/REPLAN/wider-search/retry; jeden boundary może zapisać dokładnie jeden `STRUCTURED_PLANNING_FAILURE` bez exception context, raw `error_message` oraz zmian solvera/DTO. TASK_SCOPE, A2/A3/A15 i wąska macierz są kompletne. PASS dotyczy exact brief `fe689a1`. Raport: `tasks/ROTA-TECHNICAL-ERROR-RECOVERY-UX/round_01/tests/tests_r2.txt`. Bez testów produktu, pełnej regresji, symulatorów i benchmarków. **Poprzednie przekazanie:** **R1 CORRECTED — IMPLEMENTATION HOLD, tylko literalny re-check 3 punktów.** Domknięto jedyną lukę z R1: `PlanningResult(status=TECHNICAL_ERROR)` jest HTTP 200, więc wspólny boundary `api/routers/schedule.py::_planning_result_out` (albo jeden równoważny helper używany przez wszystkie obecne PLAN/REPLAN/wider-search/retry) jest teraz jawnie w TASK_SCOPE i ma zapisać dokładnie jeden sanitizowany wpis runtime logu także bez exception context. Kategoria wpisu: `STRUCTURED_PLANNING_FAILURE`; nie wolno kopiować surowego `PlanningResult.error_message`, zmieniać solvera/DTO ani rozrzucać loggerów po engine/plan_ops/handlerach. Pozostały kontrakt bez zmian: reuse T021c/T060, jeden rotating `runtime-errors.log` (1 MiB + 4 kopie), LOCAL_WINDOWS default / CENTRAL_SERVICE `ROTA_LOG_DIR`, istniejący ZIP diagnostyczny, prosty komunikat restartu, disabled `Kontakt ze wsparciem`, bez telemetryki/support backendu/activity logu. Codex ma sprawdzić tylko: (1) czy jeden boundary obejmuje wszystkie cztery ścieżki; (2) czy structured TECHNICAL_ERROR daje dokładnie jeden sanitizowany wpis bez raw error_message i bez exception context; (3) czy TASK_SCOPE/acceptance są kompletne. Bez ponawiania inventory T021c/T060. |
| ROTA-T065 | CC | Architekt | — (finding, brak briefu) | `3d9600f` (finding) | OWNER_DECISION_NEEDED | **OWNER_CONFIRMED 2026-09-10 — NIE ODŁOŻONE: podjąć po ustabilizowaniu modułu Ochrona.** Pełny finding: `arch/FINDING_2026-09-03_STANDARDOWY_EMPLOYEE_CATEGORIES.md` (istniał lokalnie od 2026-09-03, dopiero teraz scommitowany — architekt nigdy wcześniej go nie widział). Sedno: `planning_regime=ORDINARY` ("Standardowy") to dziś dosłownie skopiowana logika Ochrony, bez własnej tożsamości. Realny cel: sklep żony Pawła — zmiany 5-12 i 12-19 pon-pt, 5-14 w sobotę, z TRZEMA niezależnie rotującymi warstwami załogi: kierownicy, załoga, uczniowie (czwarta, "ekipa sprzątająca", jawnie niepewna). Zweryfikowane dwuprzebiegowo (agent czytający pełne pliki + spot-check CC przez `where.py` i bezpośredni odczyt `fairness.py`/`constraints.py`): (1) dowolne godziny zmian per dzień tygodnia — JUŻ DZIAŁA, `StandardShift` ma wolne `start_time`/`end_time`, zero zmian kodu; (2) zamknięta niedziela — JUŻ DZIAŁA innym mechanizmem (brak dnia 7 w `active_weekdays`, nie przez `CalendarDay.holiday`); (3) niezależne, nie-zamienne kategorie pracowników — POTWIERDZONE BRAKUJĄCE, brak jakiegokolwiek częściowego mechanizmu w `rota/`. Mapa dotknięcia (faktyczna, nie projekt): `rota/domain.py` (wartość kategorii na `SiteMembership`), `rota/planning/shift_catalog.py` (przeniesienie kategorii z `StandardShift` do `ShiftDemand`), `rota/planning/eligibility.py` (nowa twarda brama kategorii, analogiczna do istniejących), `rota/planning/solver.py` (prawdopodobnie bez zmian strukturalnych — jedna brama eligibility może wystarczyć). Dodatkowo, z późniejszej uwagi Pawła: potrzebny NOWY WYDRUK z grupowaniem po kategorii (`schedule_export.py::_build_rows` dziś czysto alfabetyczne, jedna płaska tabela) — to osobny kawałek zakresu, nie automatycznie pokryty zmianą backendu. Skala: architektoniczna (dotyka modelu domeny, bramy eligibility i generowania katalogu — trzy pliki), ale węższa niż przebudowa solvera; nowe pola addytywne, wzorowane na istniejących (`can_work_24h`, `membership_kind`, `catalog_kind`). Otwarte, nierozstrzygnięte w findingu: czy kategoria żyje na `Employee` czy `SiteMembership`; czy "ekipa sprzątająca" wchodzi w zakres teraz; interakcja kategorii z `EXTERNAL_SUPPORT`/`can_work_24h`/`SiteRule`; dokładny układ wydruku; czy to jeden Task czy dwa (backend+solver vs. wydruk). CC nie projektuje rozwiązania — architekt przygotowuje brief, gdy Paweł da sygnał (po ustabilizowaniu Ochrony). |
