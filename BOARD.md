# BOARD.md — kolejka przekazań CC ↔ Codex

Nie czytane automatycznie jak AGENTS.md — trzeba wprost polecić "na początku
czytaj BOARD.md" (patrz AGENTS.md). To jest wyłącznie dziennik przekazania:
kto, co, na jakim SHA, gdzie leży raport. Żadnych ustaleń produktowych,
żadnych decyzji właściciela — te nadal trafiają do brief.md/kontraktu danego
Tasku. PR pozostaje realnym wyzwalzem pracy; ten plik tylko rejestruje
przekazanie, żeby nie trzeba było ręcznie przeklejać wiadomości między CC a
Codexem.

Statusy (dokładnie cztery, nic więcej):

- `READY_FOR_CODEX` — CC skończył, czeka na audyt.
- `CODEX_IN_PROGRESS` — Codex audytuje.
- `CODEX_REPORTED` — raport gotowy pod wskazaną ścieżką.
- `OWNER_DECISION_NEEDED` — audyt utknął na decyzji właściciela.

Nowy wiersz dopisuje autor przekazania; zmianę statusu na kolejny etap
wpisuje ten, kto ten etap kończy. Zamknięty wiersz (merge/decyzja, ostatni
status rozstrzygnięty) usuwa z tego pliku ten, kto go zamyka — pełna
historia i tak zostaje w `git log -p BOARD.md`, więc nic nie ginie, tylko
plik nie rośnie w nieskończoność (2026-08-29, OWNER_CORRECTED: wcześniejsza
wersja tej reguły mówiła "nie kasować wierszy" — celowo zmienione).

| ID | Autor | Odbiorca | Branch | Exact SHA | Status | Wiadomość |
|---|---|---|---|---|---|---|
| ROTA-SOLVER-RHYTHM-VS-TARGET | CC | Architekt | — (brak brancha, brak brief.md) | `17c2657` (finding) | OWNER_DECISION_NEEDED | **Prośba o brief: solver poświęca rytm D/N/W/W i unikanie 3 zmian pod rząd dla precyzji equity w target_hours, nawet gdy target strukturalnie nieosiągalny.** Pełny opis: `arch/FINDING_2026-09-05_SOLVER_RHYTHM_VS_TARGET_PRECISION.md` (main@`17c2657`). Skrót: obiekty mają zwykle nadwyżkę zatrudnienia (bufor na urlopy/L4), więc suma `target_hours` przewyższa realne zapotrzebowanie — część niedoboru jest z góry nieunikniona. Mimo to `add_target_equity_fairness` (`fairness.py:139-168`) dalej wyrównuje procent realizacji między pracownikami z dokładnością co do godziny, kosztem rytmu D/N/W/W i unikania 3 zmian pod rząd (`DN_RHYTHM_REWARD_WEIGHT`/`THIRD_CONSECUTIVE_SHIFT_PENALTY_WEIGHT` = 1, matematycznie zdominowane przez `TARGET_DEVIATION_WEIGHT`+equity, `solver.py:493-514/549-554`, OWNER_CORRECTED 2026-08-25). OWNER: koordynator w realnej pracy **nigdy** nie robi 3 zmian pod rząd — to bliżej HARD niż SOFT. Program już ma na to gotowy mechanizm: `DECISION_REQUIRED` (`engine.py`, dziś używany dla REST-01/LOAD-01/MEMBERSHIP-01), ale "3 zmiany pod rząd" nie jest do niego podłączone. 3 otwarte pytania projektowe w dokumencie (poziom priorytetu dla 3-zmian-pod-rząd vs ogólnego rytmu, czy equity potrzebuje pasma tolerancji, czy to jeden mechanizm czy dwa). CC nie projektuje rozwiązania — dotyka solvera, wymaga architekta. |
| ROTA-CORRECTION-EFFECTIVE-FROM-DEFAULT | Codex | Architekt | — (pre-brief finding) | main@`1463188` | CODEX_REPORTED | **GOTOWE DO BRIEFU po skorygowaniu uzupełnienia CC.** OWNER_ACCEPTED 2026-09-05: przed rozpoczęciem pozostają zwykłe korekty planu z datą skutku wyliczoną z początku Assignmentu; po rozpoczęciu kod/godziny/demand/stan/freeze są niezmienne, a jedynym wyjątkiem jest zapisanie innego pracownika, który faktycznie wykonał całą służbę, z obowiązkową przyczyną i historią. Backend jest ownerem. **Potwierdzone ponownie 2026-09-06 (na wyraźną prośbę ownera, sprawdzone dla Korekty ręcznej po Plan/Replan/Przelicz Plan — patrz `ROTA-T057`):** `apply_manual_correction` (`manual_edit.py:297-346`) nie używa solvera w ogóle (nie ma tu problemu ginącej zmiennej decyzyjnej z T057), ale ma dokładnie tę samą lukę z innego powodu — jedyna kontrola to `validate_realized_preserved()`, działająca tylko dla stanu REALIZED, którego prawie nikt nie ustawia automatycznie; dla zwykłego już-odbytego, ale wciąż PLANNED przypisania nie ma żadnej kontroli czasowej, żadnego wymogu podania powodu, żadnego rozróżnienia dozwolonego wyjątku (inny pracownik faktycznie wykonał służbę) od nieautoryzowanej zmiany rdzenia służby. To nie nowy błąd, tylko potwierdzenie zakresu tego wiersza z realnego kodu, nie z domysłu. CC poprawnie znalazł `AssignmentState.REALIZED` i `validate_realized_preserved()` jako użyteczny bezpiecznik, ale nie jako kompletne rozwiązanie: stan nie jest całkiem nieużywany (obsługuje realizację szkolenia i jest czytany przez bilanse/historię świąt); zwykła korekta może dziś oznaczyć nawet przyszłe PRIMARY jako REALIZED; bezwzględna niezmienność zablokowałaby dozwoloną korektę faktycznego pracownika; restore starszej wersji omija blokadę przez utworzenie nowej gałęzi. Istniejący cutover chroni tylko wybór kandydata po REPLAN. Brief ma objąć wszystkie ścieżki, w tym restore i bezpośredni endpoint, oraz traktować REALIZED jako możliwy element, nie narzucone rozwiązanie. Pełny kontrakt zachowania: `arch/PREBRIEF_AUDIT_2026-09-05_T056_FOLLOWUPS.md`; niezależny reaudyt i reproduktory: `arch/PREBRIEF_REAUDIT_2026-09-05_HISTORICAL_SERVICE_REALIZED.md`. |
| ROTA-PRINT-IGNORES-UNACKED-DEVIATIONS | Codex | Architekt | — (pre-brief finding) | main@`e4d1504` | CODEX_REPORTED | **PROBLEM ZREDUKOWANY DO JEDNEJ CENTRALNEJ LUKI EKSPORTU — gotowy do briefu.** Korekta ręczna może utworzyć LAW, validator je zapisuje, ale endpoint PDF nie sprawdza świeżych LAW ani jednorazowego potwierdzenia i mimo to generuje wydruk. Przed każdym PDF backend ma świeżo sprawdzić dokładnie drukowany, wybrany grafik; niepotwierdzone LAW blokuje ten wydruk i wraca jako czytelna lista do potwierdzenia tylko dla tej jednej próby. Następny wydruk wymaga ponownego potwierdzenia; ostrzeżenia nie-LAW nie blokują; bezpośredni POST nie omija bramki. Legalny wybrany `WORKING` nadal wolno drukować. Nie przedstawiać REPLAN/restore jako osobnych źródeł wadliwego grafiku ani osobnych napraw — ich cykl wersji został wydzielony do `ROTA-PLAN-VS-REPLAN-LIFECYCLE`. Starszy, szerszy materiał dowodowy: `arch/PREBRIEF_REAUDIT_2026-09-05_PRINT_LIFECYCLE_UI_IDENTIFIERS.md`. |
| ROTA-T057 | CC | Codex | `task/ROTA-T057` | `6ea1dc4` | READY_FOR_CODEX | **Implementacja rozpoczęta, pierwszy mały przyrost gotowy do audytu; CC kontynuuje kolejne przyrosty równolegle, nie czeka na ten audyt.** Zamiast jednego dużego diffu na końcu, T057 jest dzielony na małe, oddzielnie audytowalne kawałki (decyzja CC po rozmowie z ownerem o koszcie/ryzyku dużej niesprawdzonej zmiany w solverze). Przyrost 1 (`6ea1dc4`): `rota/persistence/schedule_repository.py` — `get_first_shift_start`/`is_schedule_version_live`, jedyny wspólny helper granicy "ożywienia" grafiku (dokładny czas startu pierwszej rzeczywistej służby, nie data kalendarzowa), na którym oprą się wszystkie pozostałe przyrosty (skasowanie nieżywego grafiku, twarda ochrona w Przelicz Plan, blokada Przelicz Plan dla grafiku nieżywego). Czysty odczyt, zero logiki zapisu, brak zmiany zachowania istniejących ścieżek. **Przyrost 2 (`e2409d2`):** migracja bazy 13→14 (nowa tabela `plan_attempt_signatures`) + funkcje repozytorium — pamięć wszystkich pokazanych wariantów Replan w jednym podejściu, przeżywająca odświeżenie strony, zgodnie z zakresem T057 2.2. **PROŚBA O ROZSZERZENIE TASK_SCOPE:** podniesienie numeru schematu psuje 2 testy sztywno zakładające `LATEST_SCHEMA_VERSION == 13` — `tests/test_t020.py` i `tests/test_t023b.py` — żadnego z nich nie ma na liście TASK_SCOPE (tylko `test_t019b.py` jest, ten już przechodzi po zmianie zakresu). Poprawka jest mechaniczna, jedna linijka na plik (`== 13` → `== 14`), nieunikniony efekt uboczny każdej migracji bazy, nie skutek błędu w implementacji. CC świadomie NIE dotknął tych plików bez zgody, zgodnie z zasadą brief'u. **PYTANIE ARCHITEKTONICZNE przed przyrostem 3 (przebudowa PLAN):** `plan_previews.schedule_version_id` (migracja 12) jest `NOT NULL REFERENCES schedule_versions(version_id)` — baza fizycznie nie pozwala zapisać podglądu bez uprzednio istniejącej, prawdziwej wersji w `schedule_versions`. To wprost blokuje wymóg T57-01 ("pierwszy nieudany PLAN dla pustego miesiąca pozostawia 0 ScheduleVersion") dla przypadku, gdy dla (site_id, month) nie istnieje jeszcze ŻADNA wersja. Dwie widoczne opcje, obie mają realne konsekwencje: (a) poluzować kolumnę do NULL-owalnej bez wymuszonego FK dla tego jednego przypadku; (b) osobna, mniejsza tabela na podgląd "przed pierwszą wersją" zamiast rozszerzania `plan_previews`. CC nie wybiera sam — to widoczna rozwidlona decyzja projektowa, nie mechaniczna poprawka. **ARCHITECT_DECISION (2026-09-06, przekazana przez ownera — architekt nie mógł sam zapisać BOARD.md):** (1) rozszerzyć TASK_SCOPE o `tests/test_t020.py` i `tests/test_t023b.py`; (2) jeden `PlanPreview`, bez drugiego subsystemu — `plan_previews` pozostaje jedynym miejscem podglądu, nie dodawać nowej tabeli tylko dla przypadku "przed pierwszą wersją"; (3) `schedule_version_id` w `plan_previews` opcjonalne/NULL przed pierwszą akceptacją; (4) pierwsza realna ScheduleVersion powstaje dopiero przy "Użyj tego grafiku"; (5) rozpoczęte służby w Przelicz Plan chronione przez `fixed_existing_assignments`/`build_fixed_intervals`, niezależnie od bieżącej eligibility — potwierdza wymóg z ustalenia o Royal; (6) T057 NIE rozszerza się o Korektę ręczną/REALIZED ani o `ABSENCE_DECOMPOSITION_REQUIRED` — oba pozostają osobnymi, już opisanymi wątkami. CC wykonuje wg tej decyzji. **Przyrost 3 (`3d7017a`):** migracja 15 — `plan_previews.schedule_version_id` teraz opcjonalne (SQLite nie pozwala zmienić NULL-owalności kolumny w miejscu, więc tabela odtworzona), zweryfikowane ręcznie z włączonym egzekwowaniem kluczy obcych. Dwa zatwierdzone przez architekta pliki testowe (`test_t020.py`, `test_t023b.py`) oraz `test_t019b.py` (już w zakresie) zaktualizowane o nowy numer schematu (13→15, dwie migracje) — 147 celowanych testów przechodzi. **Przyrost 4 (`8a65d1c`), duży:** PLAN i REPLAN (przed pierwszą akceptacją, także po DECISION_REQUIRED) już nie tworzą żadnej ScheduleVersion — tylko preview + rosnąca pamięć pokazanych wariantów; prawdziwa pierwsza wersja powstaje dopiero w `select_candidate` przy akceptacji, czytając `effective_from` zapisane na preview. `reject_plan_preview` kończy podejście. Zaktualizowano 16 testów w 6 plikach w zakresie, odzwierciedlając nowy kontrakt (nie błąd) — każdy z komentarzem wyjaśniającym. 7 pozostałych awarii **potwierdzonych jako już istniejące przed T057** (identyczna awaria na kodzie bazowym przez `git stash`, niezwiązana z tą zmianą): 4× `NARROW_SEARCH_EXHAUSTED` (wyszukiwanie różnorodności REPLAN na współdzielonym fixture "real-object"), 1× niedeterministyczna asercja wyniku solvera (`test_t017.py` M13), 2× niepowiązane sprawdzenia kalendarza absencji/git-diff (`test_t023_checkpoint_b.py`) — nietknięte, poza zakresem T057, ale warte osobnego zgłoszenia przez Codexa. 226 celowanych testów przechodzi, ruff czyste. **ZNANA LUKA na kolejny przyrost:** REPLAN przed akceptacją nie wymusza jeszcze ≥15% różnicy względem WSZYSTKICH wcześniej pokazanych wariantów w `plan_attempt_signatures` — na razie tylko gromadzi tę pamięć, solver jeszcze jej nie odczytuje. **Przyrost 5 (`3eddc78`) — znaleziony przez RĘCZNY test ownera (E1 na wydzielonym serwerze testowym, nie teoria):** po Plan, przed akceptacją, siatka pokazywała "?" zamiast D/N. Przyczyna: `demandKindByDemandId` w frontendzie budowany wyłącznie z `GET .../schedule/{month}` demands, które wcześniej istniały tylko dlatego, że PLAN tworzył pustą wersję od razu — po przyroście 4 już nie tworzy, więc nic nie karmiło siatki. Naprawa zgodna z "jeden PlanPreview, bez drugiego subsystemu": `PlanPreview` dostał pole `shift_demands` (migracja 17), GET zwraca je zamiast pustej listy, gdy nie ma jeszcze wersji. Zweryfikowane ręcznie przez CC na wydzielonym serwerze (port 8124/5175, nie dotyka normalnego serwera ownera) przed poproszeniem o ponowny klik. 82 celowane testy przechodzą, 2 pozostałe to już zgłoszony `ROTA-REPLAN-NARROW-SEARCH-EXHAUSTED-PREEXISTING`. **Przyrost 6 — potwierdzone żywym testem ownera na serwerze T057 (nie teoria):** owner dodał pracownikowi A niedostępność (`UNAVAILABLE_24H`, 12-18.10.26) na obiekcie z już zaakceptowanym, FINAL grafikiem, zrobił Replan, a "obowiązujący" grafik pokazał pracownika A przypisanego w te dni. Zbadane do końca na dokładnie tej bazie (historia akcji + zawartość wersji + zawartość podglądu): **Replan nie jest przyczyną liczenia błędnego wyniku — solver poprawnie wyklucza A z nowych propozycji, potwierdzone również osobnym Przelicz Plan, którego zapisana propozycja poprawnie NIE zawiera A.** Prawdziwa przyczyna: dzisiejszy Replan **klonuje surową zawartość rodzica jako nową wersję "obowiązującą" ZANIM solver w ogóle policzy wynik** (ten sam mechanizm co przy Royal) — klon nigdy nie przechodzi przez niezależny walidator, więc dziedziczy stare, już nielegalne przypisania i staje się "na żywo" bez żadnego ostrzeżenia, nawet bez odchylenia. Późniejszy Przelicz Plan policzył poprawną propozycję (zweryfikowana: 0 przypisań A w dniach niedostępności), ale **nigdy nie została zaakceptowana** ("Użyj tego grafiku" nie zostało kliknięte) — stąd wrażenie "Przelicz Plan nie zadziałał", choć zadziałał poprawnie; po prostu poprawny wynik czekał obok błędnego stanu "na żywo", nierozróżnialnie dla koordynatora. To bezpośrednio potwierdza kontrakt T057 (preview nigdy nie jest `current` przed "Użyj tego grafiku") jako właściwe rozwiązanie tej dokładnej klasy problemu — nowa architektura eliminuje ją strukturalnie, nie łatką na ten jeden przypadek. **Przyrost 6 (`7c4265a`) — bezpośrednia naprawa tego dokładnego błędu:** (1) `state.cutover_at` — już żywe, zaplanowane służby są teraz twardo chronione (rezerwacja czasu niezależna od bieżącej dostępności) zamiast słabego przypinania; zweryfikowane wprost — nieobecność zgłoszona po fakcie na już rozpoczętą służbę daje teraz jawne DECISION_REQUIRED zamiast cichej reasygnacji lub cichego naruszenia. (2) Przelicz Plan (`plan_month` na istniejącej wersji) ustawia `cutover_at`, gdy grafik jest już żywy, a akceptacja zawsze tworzy dokładnie jedną nową wersję (historia zachowana dla KAŻDEJ akceptacji, nie tylko pierwszej). (3) **Replan wycofany strukturalnie dla każdego miesiąca, który ma już zaakceptowaną wersję** (HTTP 409, `ReplanNotAvailableAfterAcceptance`) — zgodnie z OWNER_RULING: Replan istnieje tylko przed pierwszą akceptacją; stary mechanizm (klonowanie rodzica jako nowy "obowiązujący" przed policzeniem, z pominięciem walidatora) to dokładnie to, co złapał żywy test ownera — naprawione przez usunięcie, nie łatanie. 10 testów zaktualizowanych (migracja z Replan-na-istniejącej-wersji na Przelicz Plan), każdy z komentarzem. 245 testów przechodzi, 4 pozostałe to już potwierdzony zestaw pre-existing. Kolejny przyrost: skasowanie zaakceptowanego-nieżywego grafiku + pełne wymuszenie 15% w Replan przed akceptacją + API/ekran (widoczność przycisków wg fazy). FIRST EXECUTION GATE E1–E5 dalej obowiązuje przed ciężkimi testami i handoffem; E4 wymaga optycznego porównania starego i nowego grafiku. Poprzedni raport R5 (`f2925c7`, `tasks/ROTA-T057/round_01/tests/tests_r5.txt`) pozostaje ważny jako PASS preimplementation. **NOWE, POTWIERDZONE ŻYWYM TESTEM ustalenie techniczne (2026-09-06, obiekt Royal, realny klik REPLAN — nie teoria kodu):** koordynator dostał surowy zrzut 11 linii `REPLAN cutover: new pre-cutover PRIMARY solved-2026-09-0X-D/N-<uuid> added` zamiast czytelnego komunikatu. Zbadane do końca, dokładna przyczyna: `check_eligibility()` (`rota/planning/solver.py:244-263`, wywoływane z `_build_slots`) sprawdza dostępność pracownika dla KAŻDEGO dnia, także już przeszłego. Nowo zgłoszona nieobecność pracownika na dzień, który już minął, powoduje, że program w ogóle nie tworzy dla niego zmiennej decyzyjnej na ten dzień — a mechanizm mający chronić już odbyte służby przy REPLAN (`solver.py:991-994`, `if key in x: model.add(x[key] == 1)`) wtedy po cichu nic nie robi, bo nie ma czego przypiąć. To NIE jest rzadki brzegowy przypadek: `AssignmentState.REALIZED` (jedyny mechanizm z twardą, niezależną od dostępności ochroną — przez `fixed_existing_assignments`/`build_fixed_intervals`) nie jest ustawiane automatycznie (patrz `ROTA-CORRECTION-EFFECTIVE-FROM-DEFAULT`), więc niemal każda już odbyta służba zostaje zwykłym PLANNED i polega wyłącznie na tym zawodnym mechanizmie. Jedna nieobecność potrafi kaskadowo rozsypać cały poprzedni tydzień, bo zwolnione miejsce zmusza solver do przetasowania też innych już odbytych zmian. **Twardy wymóg dla implementacji Przelicz Plan w T057 (rozdział 2.4 brief'u "twardo chroni... nawet gdy bieżące reguły uznałyby je dziś za niepożądane"):** już rozpoczęte służby muszą być chronione tym samym niezawodnym mechanizmem co REALIZED (`fixed_existing_assignments`/`build_fixed_intervals`, rezerwacja czasu niezależna od bieżącej eligibility), NIE przez `if key in x: model.add(x[key]==1)` na zmiennej, która może w ogóle nie istnieć. Osobno, do zbadania przy okazji: koordynator dostał też surowy techniczny błąd `ABSENCE_DECOMPOSITION_REQUIRED` z eksportu PDF na tym samym obiekcie — to jest już istniejący, osobno zaprojektowany polski komunikat (`api/routers/export.py:47`), niepowiązany z tym błędem REPLAN; może być realnym ograniczeniem biznesowym, nie badane dalej w ramach T057. **DALSZE BADANIE (na wyraźną prośbę ownera) — zwykły PLAN/dzisiejszy Przelicz Plan są GORZEJ chronione niż REPLAN, nie tak samo:** (1) poziom solvera — ścieżka bez `require_different_from_baseline` (`solver.py:1002-1005`) nie ma ŻADNEJ próby twardego przypięcia, tylko miękki cel minimalizacji łącznej liczby zmian (`build_reshuffle_count_expr`), identyczny dla przeszłości i przyszłości — nie zna nawet pojęcia cutover_at. (2) poziom zapisu — `_enforce_replan_cutover` (`plan_ops.py`) zaczyna się od `if header.parent_version_id is None: return` — czyli kontrola przy zapisie w ogóle się nie uruchamia dla wersji bez rodzica, a dokładnie taka jest wersja, na której działa dziś zwykły PLAN/Przelicz Plan (edytuje w miejscu, nigdy nie tworzy dziecka); potwierdzone przechodzącym testem `test_t23_r5_3f_initial_plan_not_subject_to_cutover_guard`. **Wniosek:** to samo uszkodzenie przeszłości, które przy REPLAN dało brzydki, ale widoczny błąd, przy zwykłym PLAN/Przelicz Plan zapisałoby się CICHO, bez żadnego ostrzeżenia — brak ochrony na obu poziomach jednocześnie. To nie jest nowy, osobny finding — to bezpośrednio potwierdza i podnosi priorytet wymogu z brief'u T057 sekcja 2.4 dla dokładnie tej ścieżki. |
| ROTA-NO-PERSISTENT-ERROR-LOG | CC | Architekt | — (brak brancha, brak brief.md) | `2f10df3` (stan sprawdzony) | OWNER_DECISION_NEEDED | **Prośba o brief: program nie zapisuje błędów na bieżąco do żadnego pliku — sprawdzone wprost, nie z pamięci.** Sprawdzone: nie istnieje `logs/errors.log` ani żaden inny plik logu błędów aplikacji; brak jakiejkolwiek konfiguracji `logging.FileHandler`/`logging.basicConfig` w `api`/`rota`. Jedyny istniejący mechanizm zapisu (`log/session.txt` przez `session_log.py`) to dziennik pracy CC/Codexa nad zadaniami, nie log błędów działającego programu. Skutek: gdy koordynator trafia na błąd (np. surowy zrzut REPLAN na obiekcie Royal, 2026-09-06), jedyny ślad to to, co akurat zobaczy na ekranie i przekaże ręcznie — nie da się sprawdzić błędu po fakcie, brak czasu zdarzenia, brak pełnego stack trace. OWNER: to jest odpowiedź na wielokrotnie powtarzane pytanie "czy CC ma diagnostykę" — nie miał, dopiero żywy klik na Royal to ujawnił. CC nie projektuje rozwiązania (wybór: plik rotowany, poziom logowania, co dokładnie logować przy RODO wrażliwych danych pracowników) — to wymaga architekta. |
| ROTA-REPLAN-NARROW-SEARCH-EXHAUSTED-PREEXISTING | CC | Codex | — (brak brancha, znalezisko przy T057) | main@`8a65d1c` (T057 branch) | READY_FOR_CODEX | **Znalezisko przy okazji triażu testów T057 — potwierdzone jako już istniejące PRZED T057, niezwiązane z tą zmianą.** Co najmniej 7 testów REPLAN na współdzielonym fixture "real-object" (`test_t009_plan_select_replan.py::test_9`, `test_audit_t009_r6.py::test_r6_replan_candidate_with_fixed_facts_can_be_selected[frozen/realized]`, `test_t031_schedule_api.py::test_replan_after_select_returns_new_candidates`) dostają `NARROW_SEARCH_EXHAUSTED` zamiast oczekiwanego `FEASIBLE`. Zweryfikowane metodą `git stash` do kodu sprzed zmian CC — identyczna awaria na oryginalnym kodzie, więc to nie regresja T057. Może to być: (a) fixture "real-object" ma zbyt ciasną obsadę, żeby REPLAN mógł znaleźć naprawdę inny wariant (fałszywy alarm testu), albo (b) realny błąd w mechanizmie różnorodności REPLAN. CC nie badał przyczyny głębiej — poza zakresem T057, zgłoszone do audytu Codexa osobno. |
| ROTA-DEVIATION-RAW-ASSIGNMENT-ID | Codex | Architekt / CC | — (pre-brief finding, zależność PRINT-BLOCK) | main@`e4d1504` | CODEX_REPORTED | **OWNER_ACCEPTED 2026-09-05: wszystkie techniczne identyfikatory mają zniknąć z całego UI koordynatora.** CC nie wykonał jeszcze pełnego inventory. Reaudyt potwierdził dalsze powierzchnie: Overview drukuje employee_id; Decisions ma fallback ID/requested_by/linked action IDs/raw condition; History pokazuje coordinator/source/decision/action/rule IDs i ID w before/after; Export pokazuje revision hash, siteId w nazwie pliku i dwa kody hash na PDF; Analytics pokazuje siteId; wspólny klient renderuje surowe `body.detail`, a backend detail często zawiera wewnętrzne ID. To minimalne source inventory, nie zamknięcie runtime — CC/architekt ma dokończyć realne ścieżki wszystkich ekranów i jawnie podzielić Taski. „LAW Godziny” nadal nie wystarcza. Audyty: `arch/PREBRIEF_AUDIT_2026-09-05_T056_FOLLOWUPS.md` oraz `arch/PREBRIEF_REAUDIT_2026-09-05_PRINT_LIFECYCLE_UI_IDENTIFIERS.md`. |
