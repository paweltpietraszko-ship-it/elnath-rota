# BOARD.md — kolejka przekazań CC ↔ Codex

Nie czytane automatycznie jak AGENTS.md — trzeba wprost polecić "na początku
czytaj BOARD.md" (patrz AGENTS.md). To jest wyłącznie dziennik przekazania:
kto, co, na jakim SHA, gdzie leży raport. Żadnych ustaleń produktowych,
żadnych decyzji właściciela — te nadal trafiają do brief.md/kontraktu danego
Tasku. PR pozostaje realnym wyzwalzem pracy; ten plik tylko rejestruje
przekazanie, żeby nie trzeba było ręcznie przeklejać wiadomości między CC a
Codexem.

Statusy (dokładnie cztery, nic więcej):

- `READY_FOR_CODEX` — CC skończył, czeka na audyt.
- `CODEX_IN_PROGRESS` — Codex audytuje.
- `CODEX_REPORTED` — raport gotowy pod wskazaną ścieżką.
- `OWNER_DECISION_NEEDED` — audyt utknął na decyzji właściciela.

Nowy wiersz dopisuje autor przekazania; zmianę statusu na kolejny etap
wpisuje ten, kto ten etap kończy. Zamknięty wiersz (merge/decyzja, ostatni
status rozstrzygnięty) usuwa z tego pliku ten, kto go zamyka — pełna
historia i tak zostaje w `git log -p BOARD.md`, więc nic nie ginie, tylko
plik nie rośnie w nieskończoność (2026-08-29, OWNER_CORRECTED: wcześniejsza
wersja tej reguły mówiła "nie kasować wierszy" — celowo zmienione).

| ID | Autor | Odbiorca | Branch | Exact SHA | Status | Wiadomość |
|---|---|---|---|---|---|---|
| ROTA-BACKEND-TASK-SCOPE-FORMAT-DRIFT | Codex | Architekt/CC | `task/ROTA-BACKEND-TASK-SCOPE-FORMAT-DRIFT` | audit `b8a2a7a` na corrected brief/tests `df2e859fb263c92d343791d1b9e2a57127b34dda` | CODEX_REPORTED | **PASS PREIMPLEMENTATION R3.** Korekta jest kompletna: invalid UTF-8 ma kontrolowany kontrakt CLI bez tracebacku/DIFF_SCOPE, osobny nagłówek Markdown zamyka scope, a zredukowane piony CLI zachowują pełne klasy w testach bezpośrednich. `backend.py` nadal niezmieniony. Baseline acceptance + audit repro: 26 PASS / 21 oczekiwanych FAIL ujawniających stan przed poprawką; ruff i diff-check PASS. Raport: `tasks/ROTA-BACKEND-TASK-SCOPE-FORMAT-DRIFT/round_01/tests/tests_r3.txt`. Implementacja może rozpocząć się wyłącznie według briefu `df2e859`; finalny audyt użyje `audit_r2_repro.py`, modułu acceptance i rzeczywistego CLI. |
| ROTA-EMPLOYEE-DETAIL-SIZE-LIMIT | CC | Architekt | — (finding, brak briefu) | `frontend/src/screens/EmployeeDetail.tsx` @ `task/ROTA-T065-ORDINARY-TIME-AVAILABILITY@9f3c86d`, 1055 linii | OWNER_DECISION_NEEDED | **Plik przekracza limit 600 linii (był 988 na `main` już przed dzisiejszą sesją, teraz 1055).** Wielokrotne kolejne Taski (T021 audyty, T053, T062, T064, CONFIGURABLE-ROLES, teraz ORDINARY-TIME-AVAILABILITY) dokładały tu funkcjonalność wymienioną wprost w swoich WHERE_MAP, każdy przyrost osobno mały i uzasadniony, ale plik nigdy nie był dzielony. Paweł zauważył to w trakcie dzisiejszej sesji przy ORDINARY-TIME-AVAILABILITY; owner decision: dokończyć bieżący brief minimalnie (zrobione), zgłosić przekroczenie osobno zamiast łączyć z bieżącym Taskiem. Potrzebna decyzja architekta/właściciela: osobny Task refaktoryzujący podział na mniejsze komponenty (np. macierz dostępności, formularz nieobecności, log historii jako osobne pliki), zanim kolejny Task znowu tu doda funkcjonalność. |
| ROTA-ORDINARY-POSITION-FIXTURE-FALLOUT | CC | Codex | `task/ROTA-ORDINARY-POSITION-FIXTURE-FALLOUT` | impl `ba121a0` (base `main`, brak briefu — mechaniczny bugfix, owner-delegowana decyzja) | READY_FOR_CODEX | **Naprawa mechanicznej klasy z poprzedniego wpisu `ROTA-PREEXISTING-FAILURES-FULL-COUNT` (usunięty, zastąpiony tym wierszem).** 64 pre-existing failures na `main` → 33. Naprawione: (1) 28 testów — fixture konstruuje ORDINARY membership bez roli, trafia w inwariant `_require_ordinary_position_still_valid` z CONFIGURABLE-ROLES; przełączone na OCHRONA w 7 plikach (`test_t011_a/b/c/d/e_happy/e_hard_stop.py`, `test_t017.py`) — ten sam sprawdzony wzorzec co wcześniejsze poprawki tego dnia. (2) 3 testy — martwy literał `LATEST_SCHEMA_VERSION == 20` (migracja 21 już scalona) w `test_t019b.py`/`test_t020.py`/`test_t023b.py`. Sprostowanie własnej wcześniejszej pomyłki: `test_audit_r25_findings.py` NIE należy do grupy (1) jak wcześniej błędnie zapisano — zweryfikowane bezpośrednio, to inny root cause (dn_semantics), zostawione nietknięte. Pozostałe 33 (dn_semantics ~28, fałszywe alarmy "no coupling" 3, 1 samo-unieważniający się diff-proof) świadomie odłożone do ODLOZONE.md — owner explicit "nie wiem co z tym, decyduj sam", CC zdecydował naprawić tylko klasy mechaniczne/pewne. Ruff czysty na 10 plikach. |
| ROTA-FILES-REFACTOR-L1-DB | Codex | CC | `task/ROTA-FILES-REFACTOR-L1-DB` | audit `30efc02` of impl `135ef0e` (base `8a9a6df`; `tasks/ROTA-FILES-REFACTOR-L1-DB/round_01/tests/tests_r1.txt`) | CODEX_REPORTED | **PASS R1 na dokładnym `135ef0e`.** Niezależne porównanie wykonywalnego AST potwierdziło identyczność wszystkich migracji i orkiestratora, brak zgubionych/zdublowanych właścicieli oraz prosty DAG importów. Stary monolit i nowy podział tworzą byte-for-byte ten sam `sqlite_master`, zachowano `db._MIGRATION_N`, kolejność 1..21 i monkeypatch `MIGRATIONS`; wszystkie pliki <600 linii. Celowane testy migracji i RODO: 16 passed; ruff i diff-check czyste. Pełnej regresji, zamrożonych symulatorów ani benchmarków nie uruchamiano — dla czystego przeniesienia nie były potrzebne ani autoryzowane. Jedyna różnica poza lokalizacją to dostosowany docstring migracji 18, TECHNICAL_ONLY. |
| ROTA-T023-ORDINARY-SITE-FIXTURE-MISMATCH | CC | Codex | — (finding, brak briefu, znalezione podczas `task/ROTA-T065-PRINT-GAP`) | `tests/test_t023.py` linia 58/149 na `task/ROTA-T065-PRINT-GAP` (base `main@45c021b`) | READY_FOR_CODEX | **Ten sam wzorzec co `ROTA-ORDINARY-POSITION-FIXTURE-FALLOUT`, w innym pliku.** `tests/test_t023.py::_setup`/`_setup_two_sites` konstruuje `SITE`/`SITE-B` jawnie jako `Site(..., planning_regime=SitePlanningRegime.ORDINARY)`, ale `tests/test_t023_checkpoint_c.py` (8 testów: T23-35, T23-40..46a) testuje ten Site kodami czysto ochroniarskimi (D1, C3, `base_regime="24h"`, PLAN/WYK). Działało wcześniej tylko dlatego, że `schedule_export.py`/`site_repository.py` nie znały `planning_regime`. Po PRINT-GAP (`_assemble_export_model` poprawnie kieruje ORDINARY Site do `model.ordinary_rows`, nie `model.rows`) tych 8 testów dostaje pusty `model.rows` → `StopIteration`. Zweryfikowane bezpośrednio: scoped run tych 8 testów + reszty plików dotykających `schedule_export.py` (`test_export_unacknowledged_law.py`, `test_t021_export_api.py`, `test_t023_checkpoint_b.py`, `test_t023b.py`, `test_t026.py`, `test_t041_checkpoint_b.py`, `test_t047_print_export.py`, `test_vertical_full_stack.py`) — tylko te 8 nowo czerwone, reszta bez zmian (poza dwoma już znanymi z `ROTA-ORDINARY-POSITION-FIXTURE-FALLOUT`: `test_t23_54` diff-proof i `LATEST_SCHEMA_VERSION==20` literał, oba na osobnym, jeszcze niezmergowanym branchu naprawy). Poza TASK_SCOPE PRINT-GAP (`test_t023.py`/`test_t023_checkpoint_c.py` nie są w jego WHERE_MAP) — CC nie naprawia sam. Sugerowana mechaniczna naprawa: `SITE`/`SITE-B` w `test_t023.py::_setup`/`_setup_two_sites` → `SitePlanningRegime.OCHRONA`, ten sam sprawdzony wzorzec z fixture-fallout. |
| ROTA-THIRD-CONSECUTIVE-SHIFT-ORDINARY-SCOPE | CC | Architekt | — (finding, brak briefu) | trace na `main@7fdbaee` | OWNER_DECISION_NEEDED | **HARD reguła THIRD-CONSECUTIVE-SHIFT-01 (max 2 dni robocze pod rząd, T058, `add_max_two_consecutive_primary_shift_constraint`) nie jest i nigdy nie była rozróżniana per regime — potwierdzone bezpośrednim trace, zero regime-gate w którymkolwiek miejscu.** Lokalizacje: `rota/planning/constraints.py::add_max_two_consecutive_primary_shift_constraint` (linie 699-722), wywoływana bezwarunkowo z `rota/planning/solver.py` linia ~1193; niezależne lustro walidatora `rota/planning/validator_checks_patterns.py::_check_third_consecutive_shift` — również bez regime-gate. Zaprojektowane pod zmęczeniową logikę OCHRONY (strażnicy), ale blokuje też ORDINARY (handel). Żywy repro: obiekt FF (ORDINARY), październik 2026 — po dodaniu urlopów kilku pracownikom `plan_ops.plan_month(...)` zwrócił `status='THIRD_CONSECUTIVE_SHIFT_BLOCKED'` z komunikatem wskazującym 9 employee_id, mimo że koordynator miał realną, sensowną alternatywę (wysłać kogoś jeszcze na urlop zamiast trzeciego dnia pod rząd). **Ustalona z Pawłem wprost reguła produktowa (plain-language, potwierdzone "Tak" po restatement)**: dla ORDINARY koordynator układa zmiany codziennie w praktyce — jedyne HARD limity, jakie powinny obowiązywać, wynikają z kodeksu pracy (REST-01/WEEKLY-REST-01, bez zmian, zostają); jeśli brakuje godzin do rozdysponowania, remedium koordynatora to wysłanie kogoś na urlop, nie sztuczny blok systemu. Wniosek: THIRD-CONSECUTIVE-SHIFT-01 powinna stać się regułą wyłącznie OCHRONA — dokładnie ten sam wzorzec, co już wdrożony `SHIFT-24-01`/`can_work_24h` (ROTA-T065-ORDINARY-TIME-AVAILABILITY, OCHRONA-only). To zmiana solvera/granicy FEASIBLE — CC nie implementuje sam, zgłaszane do pełnego procesu (brief + Codex + architekt) zgodnie ze standardową zasadą tego projektu. |
| ROTA-DELEGACJA-ABSENCE-KIND | CC | Architekt | — (owner feature request, brak briefu) | request na `main@eaec41f` | OWNER_DECISION_NEEDED | **Nowy Task potrzebny: nowy rodzaj nieobecności DELEGACJA.** Właściciel (Paweł) zgłosił wprost i CC potwierdził restatementem ("tak"): (1) nowy `AvailabilityKind.DELEGACJA` obok istniejących (`LEAVE_GRANTED`/`SICK_LEAVE`/`DAY_SHIFT_OFF`/`UNAVAILABLE_24H` — zob. `rota/domain.py` linia 33), dostępny dla OBU regime'ów (ORDINARY i OCHRONA). (2) HARD-blokuje automatyczne przydzielenie zmiany w macierzystym obiekcie na ten dzień — identyczny mechanizm blokowania jak dziś `LEAVE_GRANTED`/`SICK_LEAVE` (`eligibility.py`/`validator.py`). (3) Każdy Site ma domyślną liczbę godzin delegacji w swoich ustawieniach (przykład właściciela: sklep żony = 7h), ale przy wpisywaniu konkretnej delegacji koordynator może tę wartość nadpisać jednorazowo (przykład: 8h przy dalszej trasie) — to jest per-instancja wartość z Site-owym domyślnym podpowiedzeniem, nie sztywna stała. (4) Odwrotnie niż `LEAVE_GRANTED`/`SICK_LEAVE` (które zmniejszają wymagany limit godzin — zob. `rota/planning/absence.py` `EXCUSED_ABSENCE_HOURS_PER_DAY`), godziny delegacji mają liczyć się jako godziny REALNIE przepracowane — wchodzą do sumy "Godz." na wydruku (`schedule_export.py`) i do kwartalnego rozliczenia (`rota/balance.py`), analogicznie do realnych godzin pracy, nie do kategorii excused-absence. (5) Na wydruku ORDINARY potrzebny osobny symbol/oznaczenie (właściciel zaproponował "D") — **do zweryfikowania przez architekta/Codex, czy "D" nie koliduje z istniejącymi oznaczeniami D/N zmian w OCHRONA** (`_ordinary_absence_word`/`_draw_ordinary_row` w `schedule_export.py` już używają pogrubionej/przerywanej ramki dla Urlop/L4 — analogiczny wizualny kod dla DELEGACJA do zaprojektowania), CC nie przesądza symbolu. Dotyka: `rota/domain.py` (nowy enum member), migracji DB (nowa wartość w CHECK/ENUM jeśli jest), `rota/planning/eligibility.py`/`validator.py` (blokada), `rota/planning/absence.py`/`rota/balance.py` (odwrotna logika liczenia godzin — dodaje zamiast odejmować), `schedule_export.py` (nowy symbol na wydruku obu regime'ów), prawdopodobnie nowego pola "godziny delegacji" w ustawieniach Site i w formularzu dodawania nieobecności we frontendzie. To zmiana modelu domeny + granicy solvera + kontraktu wydruku — CC nie implementuje sam, potrzebny pełny brief + Codex + architekt. |
