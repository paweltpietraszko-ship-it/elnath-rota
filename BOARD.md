# BOARD.md — kolejka przekazań CC ↔ Codex

Nie czytane automatycznie jak AGENTS.md — trzeba wprost polecić "na początku
czytaj BOARD.md" (patrz AGENTS.md). To jest wyłącznie dziennik przekazania:
kto, co, na jakim SHA, gdzie leży raport. Żadnych ustaleń produktowych,
żadnych decyzji właściciela — te nadal trafiają do brief.md/kontraktu danego
Tasku. PR pozostaje realnym wyzwalzem pracy; ten plik tylko rejestruje
przekazanie, żeby nie trzeba było ręcznie przeklejać wiadomości między CC a
Codexem.

Statusy (dokładnie cztery, nic więcej):

- `READY_FOR_CODEX` — CC skończył, czeka na audyt.
- `CODEX_IN_PROGRESS` — Codex audytuje.
- `CODEX_REPORTED` — raport gotowy pod wskazaną ścieżką.
- `OWNER_DECISION_NEEDED` — audyt utknął na decyzji właściciela.

Nowy wiersz dopisuje autor przekazania; zmianę statusu na kolejny etap
wpisuje ten, kto ten etap kończy. Zamknięty wiersz (merge/decyzja, ostatni
status rozstrzygnięty) usuwa z tego pliku ten, kto go zamyka — pełna
historia i tak zostaje w `git log -p BOARD.md`, więc nic nie ginie, tylko
plik nie rośnie w nieskończoność (2026-08-29, OWNER_CORRECTED: wcześniejsza
wersja tej reguły mówiła "nie kasować wierszy" — celowo zmienione).

| ID | Autor | Odbiorca | Branch | Exact SHA | Status | Wiadomość |
|---|---|---|---|---|---|---|
| ROTA-T065 | Codex | Architekt | `main` (finding + zweryfikowany scope, brak briefu) | finding `3d9600f`; scope `66181cf`; re-check `c60f944` | CODEX_REPORTED | **ARCHITEKT — OWNER doprecyzował krytyczną zasadę: NIE tworzyć drugiej logiki, drugiego przebiegu ani drugiego solvera.** T065 NIE obejmuje `UCZEN` ani ekipy sprzątającej. Obejmuje tylko dwie rozłączne role: `KIEROWNIK` oraz `SPRZEDAWCA/ZALOGA`; zwykła obsada/rotacja odbywa się wyłącznie w tej samej roli. Jeżeli kierownik ma pracować jako sprzedawca, solver NIE podejmuje takiej decyzji i NIE wykonuje fallbacku. To jawna decyzja koordynatora: koordynator dopisuje tę osobę do załogi istniejącym mechanizmem, po czym kolejny zwykły PLAN/REPLAN działa na zaktualizowanej obsadzie. `EXTERNAL_SUPPORT` zachowuje dokładnie tę samą filozofię: niczego nie obserwujemy, nie szukamy automatycznie, nie uruchamiamy osobnego przebiegu ani osobnego mechanizmu doboru; program czeka na dopisanie kolejnej osoby do obsady przez koordynatora, a potem używa normalnego solvera. Jeśli Architekt nie zna istniejącego flow dopisywania osoby/obsady lub integracji z solverem, ma NAJPIERW przeczytać kod i użyć istniejącego mechanizmu, nie projektować nowego. Skład ról jest per obiekt: część sklepów ma tylko sprzedawców, inne wymagają kierownika + sprzedawcy; w sklepie referencyjnym jest 4 kierowników do wyboru. Solver ma układać obie pule na wspólny miesiąc według ogólnych reguł czasu pracy dla dorosłego handlu, nie profilu Ochrona. Grafiki historyczne są niemodyfikowalne — HARD. `UCZEN` pozostaje osobnym przyszłym Taskiem z własnym reżimem prawa pracy. **NOWE USTALENIE ARCHITEKTA po odczycie kodu 2026-09-13:** backend katalogu zmian JUŻ obsługuje arbitralne pełnogodzinne zmiany 7–9h jako `ShiftCatalogKind.OTHER/INNY`; nie tworzyć drugiego generatora ani osobnego modelu godzin. Problem jest w semantyce/konfiguracji `ORDINARY`: obecny `SiteShiftCatalog` nadal wystawia `Rodzaj = Dniówka/Nocka`, a pusty katalog startuje z ochroniarskim defaultem 06:00–18:00. Dla sklepu trzeba wykorzystać TEN SAM `StandardShift`/`generate_catalog_demands`, ale dać właściwy profil bazowy/kody zmian sklepowych i nie dopuścić do przypadkowego dziedziczenia ochroniarskiej semantyki `D/N` (obecne eligibility ma reguły zależne od `ShiftKind.N`, m.in. DAY_ONLY i ograniczenia shift-kind). Sklep referencyjny OWNERA: pon–pt 05:00–12:00 i 12:00–19:00, sobota 05:00–14:00, niedziela zamknięta; to ma być edytowalna konfiguracja obiektu, nie logika zaszyta w solverze. **OWNER DECISION 2026-09-13 — ZMIANY SKLEPOWE SĄ DANYMI, NIE ENUMEM:** trzeba od początku obsłużyć również sklepy całodobowe i nietypowe układy zmian. Nie wolno wprowadzać sztywnego enumu typu `RANO/POPOŁUDNIE/NOC` ani kodów o semantyce zaszytej w solverze. Definicja zmiany dla `ORDINARY` ma być konfigurowalnym rekordem obiektu: techniczny identyfikator + start + koniec + dni aktywności + wymagana rola/liczba osób; przedział może przechodzić przez północ i mieć dowolną wspieraną pełnogodzinną długość. Generator przekłada te rekordy na zwykłe `ShiftDemand`, a TEN SAM solver pracuje na konkretnych `start_datetime/end_datetime` i roli bez specjalnego przebiegu dla sklepu. **OWNER doprecyzował: solver ani użytkownik nie potrzebują symboli zmian.** Operacyjnie ma istnieć konkretne wystąpienie typu `Poniedziałek 05:00–12:00`, `Wtorek 10:00–18:00`; także „środki zmian” są po prostu kolejnymi przedziałami czasu. Kod/nazwa zmiany nie jest wymaganym elementem modelu biznesowego i nie może uruchamiać żadnej logiki. W przyszłym PDF sklepowym komórki mają pokazywać konkretne godziny (np. `5–12`, `10–18`), nie sztuczne symbole rodzin zmian. Konfiguracja referencyjna 05–12/12–19/05–14 jest tylko przykładem startowym jednego sklepu, nie kontraktem systemu. **OWNER DECISION 2026-09-13 — PODZIAŁ TASKÓW:** T065 NIE projektuje ani nie przerabia PDF poza ewentualnym zachowaniem niezbędnych danych historycznych. Sklepowy PDF jest osobnym, zależnym Taskiem z własną logiką prezentacji dla `ORDINARY`; nie wolno wciskać sklepu w ochroniarski kontrakt `base_regime 12h/24h`, kody `D1..D5/N1..N5`, D6+/N6+, S1/U*/C* ani rodzinę D/N. Wspólna może pozostać infrastruktura eksportu/PDF i źródło tej samej zamrożonej ScheduleVersion, ale logika kodów, legendy, grupowania ról i układu sklepu jest osobna. To nadal NIE oznacza drugiego solvera ani drugiego przebiegu planowania. |
