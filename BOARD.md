# BOARD.md — kolejka przekazań CC ↔ Codex

Nie czytane automatycznie jak AGENTS.md — trzeba wprost polecić "na początku
czytaj BOARD.md" (patrz AGENTS.md). To jest wyłącznie dziennik przekazania:
kto, co, na jakim SHA, gdzie leży raport. Żadnych ustaleń produktowych,
żadnych decyzji właściciela — te nadal trafiają do brief.md/kontraktu danego
Tasku. PR pozostaje realnym wyzwalzem pracy; ten plik tylko rejestruje
przekazanie, żeby nie trzeba było ręcznie przeklejać wiadomości między CC a
Codexem.

Statusy (dokładnie cztery, nic więcej):

- `READY_FOR_CODEX` — CC skończył, czeka na audyt.
- `CODEX_IN_PROGRESS` — Codex audytuje.
- `CODEX_REPORTED` — raport gotowy pod wskazaną ścieżką.
- `OWNER_DECISION_NEEDED` — audyt utknął na decyzji właściciela.

Nowy wiersz dopisuje autor przekazania; zmianę statusu na kolejny etap
wpisuje ten, kto ten etap kończy. Zamknięty wiersz (merge/decyzja, ostatni
status rozstrzygnięty) usuwa z tego pliku ten, kto go zamyka — pełna
historia i tak zostaje w `git log -p BOARD.md`, więc nic nie ginie, tylko
plik nie rośnie w nieskończoność (2026-08-29, OWNER_CORRECTED: wcześniejsza
wersja tej reguły mówiła "nie kasować wierszy" — celowo zmienione).

| ID | Autor | Odbiorca | Branch | Exact SHA | Status | Wiadomość |
|---|---|---|---|---|---|---|
| ROTA-T053 | Architect | Owner | `main` (merged PR #11) | `c12317983794bfe2876530e42d6dada035632a33` | CODEX_REPORTED | **PASS merytoryczny.** `Room.tsx` jest jednym ownerem `workingMonth`, waliduje `YYYY-MM` z miesiącem 01..12, zapisuje do localStorage, więc reload i ponowne wejście/zmiana Site zachowują miesiąc. `MonthlyPlanning`, `Analytics`, `EmployeeDetail` i `Export` używają wspólnej wartości; `Decisions` pozostaje wyjątkiem. `Export` nie utrzymuje drugiego `periodLabel` state. Finalny niezależny audit R4: build PASS + 3/3 Playwright; późniejszy merge T052 nie naruszył tego ownera ani przepływu miesiąca. |
| ROTA-T055 | Architect | Codex | `main` (brief only; implementation branch not created) | `ff3e20bcf0813282be5e7bdaffde53e084644cc1` | CODEX_REPORTED | **PREIMPLEMENTATION AUDIT.** Brief: `tasks/ROTA-T055/brief.md`. Cel: uruchomić istniejące `IndependentValidationReport.warnings` bez nowego systemu. Architektura: `open_month()` na zwykłym GET uruchamia read-only `validate()` na current snapshot i dołącza `report.warnings` do istniejącego `MonthViewOut.warnings`; istniejący banner `MonthlyPlanning` już je renderuje. Bez nowych pól write-response, bez persistence warningów, bez DB/schema/statusów, bez nowych reguł SOFT. Wszystkie istniejące validator warnings wychodzą naraz. Solver-side `DAY_SHIFT_OFF-01 SOFT` zostaje, bo obsługuje niezapisany candidate preview przed `select_candidate`. Prośba do Codexa: sprawdzić minimalność i poprawność tego read path; test nie tworzy kontraktu. **CC merytoryczna ocena briefu (2026-09-04, przed implementacją): PASS.** Zweryfikowano w kodzie, nie na słowo: `open_month()` (`rota/application/open_month.py:49`) rzeczywiście już ma `state.existing_assignments` z `assemble_planning_state()`, drugie assemblowanie nie jest potrzebne; `validate()` (`rota/planning/validator.py:724`) jest czysto obliczeniowe — brak zapisów/materializacji Deviation, bezpieczne w GET path; `MonthViewOut.warnings` + baner "Uwaga" w `MonthlyPlanning.tsx` faktycznie już renderują ten kanał. Sprawdzony brzegowy przypadek: current version z zerem przypisań wywoła `validate()` z pustą listą, co mogłoby dać fałszywe HARD violations (COVERAGE-01) — nieistotne, bo T055 czyta wyłącznie `report.warnings`, nigdy `hard_pass`/`violations`; brief poprawnie to pomija. Zakres wąski, brak nadmiarowej architektury, cztery pytania z findingu rozstrzygnięte sensownie. Brak zastrzeżeń — gotowe do audytu Codexa i mojej implementacji po PASS. **Codex preimplementation audit: PASS — READY_FOR_IMPLEMENTATION.** Raport: `tasks/ROTA-T055/round_01/tests/tests_r1.txt`. Bez kodu produktu i bez uruchamiania testów; niezależny audit implementacji ma objąć również zapisany `DAY_SHIFT_OFF-01 SOFT`. |
| ROTA-T055 | CC | Codex | `task/ROTA-T055-validator-warnings-delivery` | `e3be129a4b604f15dbe3afaa752bbf9abb6f5cd8` | CODEX_REPORTED | **Implementacja gotowa do audytu.** Minimalna zmiana zgodnie z redukcją Codexa: `rota/application/open_month.py` importuje `validate` i, wyłącznie gdy istnieje current ScheduleVersion, woła `validate(state, list(state.existing_assignments))` i dołącza `report.warnings` do istniejących `warnings`. Zero zmian w `validator.py`, `api/routers/schedule.py`, frontendzie — dokładnie jak przewidywała redukcja przed implementacją. Testy: 4 nowe (`tasks/ROTA-T055/round_01/tests/test_t055_implementation.py`) — realny reproduktor REST-01 SOFT (S1 z niedostatecznym odpoczynkiem po nocce) widoczny przez zwykły `open_month()`, ten sam warning po zamknięciu i ponownym otwarciu połączenia (brak persistence, rekomputacja), miesiąc bez current version pomija `validate()`, oraz empiryczne potwierdzenie braku zapisu (liczba wersji/przypisań niezmieniona) — plus istniejący pakiet 38 testów `open_month`/T011 bez regresji. `backend.py` na `git merge-base origin/main HEAD` = `2648836`: STATUS FAIL na trzech pozycjach REASON, wszystkie sprawdzone jako preexisting (nie dotyczą diffu T055) — SIZE_FILE `validator.py`=770, SIZE_FUNC `_check_rest`=81 (validator.py w ogóle nie dotknięty w tym tasku), SIZE_FUNC `get_month`=58 (już zaakceptowane w T054). Jedyna nowa liczba: RATIO=12,0:1 (WYMAGA_DECYZJI) — OWNER-potwierdzone. Raport: `tasks/ROTA-T055/round_01/tests/backend_output_r1.txt`. **Codex audit exact SHA: FAIL.** Transport działa, lecz nowy banner pokazuje surowy UUID pracownika, identyfikator Assignmentu i techniczny angielski tekst; istniejący `resolveWarningText()` rozpoznaje tylko ID w apostrofach, a warningi validatora zapisują je bez apostrofów. Reprodukcja przez rzeczywisty GET. Raport: `tasks/ROTA-T055/round_01/tests/tests_r2.txt` na `a5dd8c9`. Bez pełnej regresji. |
| ROTA-T055 | CC | Codex | `task/ROTA-T055-validator-warnings-delivery` | `4858dcb` (implementacja: `ec3a714`) | CODEX_REPORTED | **Naprawa R2-01 — OWNER (2026-09-04): "WSZYSTKO co widzi koordynator jest po polsku i bez nic nie mówiących symboli systemowych".** Pięć warningów walidatora (`rota/planning/validator.py`) przepisane: `DAY_ONLY-N-FALLBACK-01 SOFT`, `DAY_SHIFT_OFF-01 SOFT`, `LEAVE_PLAN-01 SOFT` przetłumaczone na polski (raw `assignment_id` usunięty tam, gdzie nie dodawał wartości — data już identyfikuje dzień; `demand_id`/`rule_version_id` zostają jako techniczne odniesienie w nawiasie dla DAY_ONLY, bo nie ma dla nich resolvera nazw); `REST-01 SOFT`/`WEEKLY-REST-01 SOFT` (już polskie od T052) — usunięta tylko para surowych assignment-id, warunki reguł bez zmian. Wszystkie pięć cytuje teraz `employee_id` w apostrofach dla istniejącego `resolveWarningText()`. **Rozszerzenie zakresu poza oryginalny EXACT_TASK_SCOPE T055, ze świadomą zgodą Pawła**: `DAY_ONLY-N-FALLBACK-01 SOFT` jest współdzielone z `engine.py`'s multi-candidate PLAN preview (`candidate=N | ...`), z zamrożonym kontraktem `tests/test_t017.py` + udokumentowanym przykładem w `tasks/ROTA-T017/brief.md` sprawdzającym stare angielskie etykiety `employee=/demand=/date=/rule_version_id=` — oba zaktualizowane do nowego polskiego brzmienia. Testy: 5/5 (`test_t055_implementation.py`, w tym bezpośrednia reprodukcja dokładnego scenariusza Codexa z tests_r2.txt) + 122 passed / 1 preexisting flake (niezwiązany, solver nondeterminism na `test_m13_first_candidate...`) w `test_t017.py`/`test_t018.py`/`test_audit_r17_findings.py`/T052/T009. `backend.py` na `git merge-base origin/main HEAD` = `2648836`: STATUS FAIL — DIFF_SCOPE na `test_t017.py`/`test_t018.py` (oczekiwane, zgoda Pawła powyżej), SIZE_FILE `validator.py`=798 (było 770), SIZE_FUNC `_check_rest`=84 (było 81), SIZE_FUNC `get_month`=58 (bez zmian, już zaakceptowane), RATIO=7,5:1 (WYMAGA_DECYZJI) — wszystkie nowe liczby OWNER-potwierdzone. Raport: `tasks/ROTA-T055/round_01/tests/backend_output_r2.txt`. **Codex wąski reaudyt: FAIL (2 findings).** UUID jest już zamieniany na nazwę, ale w UI pozostają kody `…-01 SOFT` oraz techniczne ID demandu/reguły; ponadto DAY_ONLY błędnie opisano jako „dzień wolny”, choć reproduktor nie ma DAY_SHIFT_OFF. Raport: `tasks/ROTA-T055/round_01/tests/tests_r3.txt` na `c16fad5`. Testy T055 5/5; bez pełnej regresji. |
| ROTA-T055 | CC | Codex | `task/ROTA-T055-validator-warnings-delivery` | `88d2667` (implementacja: `1fe4881`) | CODEX_REPORTED | **Naprawa R3-01/R3-02.** R3-02 (błąd treści): `DAY_ONLY-N-FALLBACK-01 SOFT` mówiło błędnie „dnia wolnego" — ta reguła dotyczy pracownika ograniczonego do zmian dziennych z wyjątkiem na nockę, nie ma nic wspólnego z DAY_SHIFT_OFF; poprawione na „ograniczenia do zmian dziennych". R3-01 (symbole systemowe): zamiast dalej przerabiać surowe stringi walidatora (na których opiera się klasyfikacja w wielu istniejących testach, w tym już dwukrotnie dotykanym w tym tasku kontrakcie T017), czyszczenie przeniesione do warstwy wyświetlania — dokładnie tam, gdzie `employee_id` jest już rozwiązywany: `MonthlyPlanning.tsx`'s `resolveWarningText()` usuwa teraz też prefiks `RULE-CODE SOFT:` oraz (tylko dla DAY_ONLY) techniczny nawias `(zapotrzebowanie..., reguła...)` — zweryfikowane regexem wprost na wszystkich 5 realnych kształtów warningów; nawiasy z godzinami/datami w REST-01/WEEKLY-REST-01/DAY_SHIFT_OFF-01/LEAVE_PLAN-01 zostają nietknięte (są znaczące dla koordynatora). Backendowe stringi (klasyfikacja `"RULE-CODE SOFT"` w testach) bez zmian. Testy: 122 passed / 1 preexisting flake (jak wcześniej) + `tsc`/ruff czyste. `backend.py` na `git merge-base origin/main HEAD` = `2648836`: STATUS FAIL — te same DIFF_SCOPE (`test_t017.py`/`test_t018.py`, już zaakceptowane; tylko `test_t017.py` zmienione w tej rundzie), nowe SIZE_FILE `validator.py`=806 (było 798), RATIO=10,8:1 (było 7,5:1); `_check_rest`=84 i `get_month`=58 bez zmian. Wszystkie nowe liczby OWNER-potwierdzone. Raport: `tasks/ROTA-T055/round_01/tests/backend_output_r3.txt`. **Codex wąski reaudyt: PASS.** R3-01/R3-02 zamknięte: widok usuwa kody i techniczne ID, UUID przechodzi w nazwę, DAY_ONLY ma poprawną przyczynę. T055 5/5, niezależne DAY_ONLY 2/2, TypeScript PASS, bez pełnej regresji. Raport: `tasks/ROTA-T055/round_01/tests/tests_r4.txt` na `d383bdc`. |
| ROTA-T055 | Architect | CC | `task/ROTA-T055-validator-warnings-delivery` | `1fe4881362b723a864f2570a86e70d6167446018` | CODEX_REPORTED | **MERYTORYCZNY FAIL — 1 wąski finding po finalnym Codex PASS.** Sam transport warningów jest poprawny i minimalny: `open_month()` + read-only `validate()` + istniejący `MonthViewOut.warnings`/banner zostają bez zmian. Problem dotyczy wyłącznie końcowego display cleanup dla `DAY_ONLY-N-FALLBACK-01 SOFT`: backendowy string ma `(zapotrzebowanie <id>, data <YYYY-MM-DD>, reguła <id>)`, ale `MonthlyPlanning.tsx::resolveWarningText()` regexem usuwa CAŁY ten nawias, więc razem z nic niemówiącymi `demand_id`/`rule_version_id` znika także operacyjnie ważna **data**. Koordynator widzi wtedy tylko np. „Jan Kowalski ma nockę mimo ograniczenia do zmian dziennych, na mocy wyjątku zmianowego”, bez wskazania której nocki/dnia dotyczy warning; przy kilku takich przypadkach komunikaty stają się nierozróżnialne. Naprawa ma być wyłącznie prezentacyjna i wąska: usuwać techniczne ID/kod, ale zachować datę zdarzenia. Bez nowych struktur, pól, warning IDs ani zmian reguły. Po poprawce wystarczy wąski reaudyt realnego DAY_ONLY warningu + TypeScript; pełna regresja niepotrzebna. |
| ROTA-T055 | CC | Architekt/Codex | `task/ROTA-T055-validator-warnings-delivery` | `46af7dc` (implementacja: `3dc5326`) | READY_FOR_CODEX | **Naprawa findingu architekta.** Zawężony regex w `resolveWarningText()` (`MonthlyPlanning.tsx`): zamiast usuwać CAŁY nawias `(zapotrzebowanie..., data..., reguła...)`, usuwa tylko `zapotrzebowanie .../reguła ...` wokół `data ...`, którą zachowuje. Zweryfikowane wprost regexem na wszystkich 5 realnych kształtów warningów — DAY_ONLY teraz kończy się na `(data 2026-10-06)`, pozostałe 4 (już wcześniej poprawne: godziny/daty w nawiasach) bez zmian. Bez zmian backendu, bez nowych struktur/pól/warning ID, reguły bez zmian — czysto poprawka regexu na warstwie wyświetlania. Testy: `tsc` czyste, 5/5 T055 (backendowe, niezmienione tą poprawką frontendową). `backend.py` na `git merge-base origin/main HEAD` = `2648836`: STATUS FAIL — te same DIFF_SCOPE/SIZE_FILE/SIZE_FUNC co poprzednio (zaakceptowane), jedyna nowa liczba RATIO=11,2:1 (było 10,8:1) — OWNER-potwierdzone. Raport: `tasks/ROTA-T055/round_01/tests/backend_output_r4.txt`. Wystarczy wąski reaudyt realnego DAY_ONLY warningu + TypeScript, zgodnie z prośbą architekta. |
