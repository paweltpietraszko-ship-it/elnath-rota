# BOARD.md — kolejka przekazań CC ↔ Codex

Nie czytane automatycznie jak AGENTS.md — trzeba wprost polecić "na początku
czytaj BOARD.md" (patrz AGENTS.md). To jest wyłącznie dziennik przekazania:
kto, co, na jakim SHA, gdzie leży raport. Żadnych ustaleń produktowych,
żadnych decyzji właściciela — te nadal trafiają do brief.md/kontraktu danego
Tasku. PR pozostaje realnym wyzwalzem pracy; ten plik tylko rejestruje
przekazanie, żeby nie trzeba było ręcznie przeklejać wiadomości między CC a
Codexem.

Statusy (dokładnie cztery, nic więcej):

- `READY_FOR_CODEX` — CC skończył, czeka na audyt.
- `CODEX_IN_PROGRESS` — Codex audytuje.
- `CODEX_REPORTED` — raport gotowy pod wskazaną ścieżką.
- `OWNER_DECISION_NEEDED` — audyt utknął na decyzji właściciela.

Nowy wiersz dopisuje autor przekazania; zmianę statusu na kolejny etap
wpisuje ten, kto ten etap kończy. Zamknięty wiersz (merge/decyzja, ostatni
status rozstrzygnięty) usuwa z tego pliku ten, kto go zamyka — pełna
historia i tak zostaje w `git log -p BOARD.md`, więc nic nie ginie, tylko
plik nie rośnie w nieskończoność (2026-08-29, OWNER_CORRECTED: wcześniejsza
wersja tej reguły mówiła "nie kasować wierszy" — celowo zmienione).

| ID | Autor | Odbiorca | Branch | Exact SHA | Status | Wiadomość |
|---|---|---|---|---|---|---|
| ROTA-T058 | Architekt | CC | `task/ROTA-T058` | brief `0709534`; R6 evidence `2466f4e`; root cause main@`0f0937a` | OWNER_DECISION_NEEDED | **ARCHITECT_APPROVED — CC może kontynuować R6-01.** `manual_edit.py` przechodzi z READ_ONLY_EVIDENCE do TASK_SCOPE wyłącznie dla T58-06/T58-15. Uzasadnienie: `fixed_existing_assignments` już dziś traktuje `frozen=True` jako istniejący marker "automat nie redystrybuuje", więc nie tworzymy nowej semantyki solvera ani danych. W `apply_manual_correction`, po `validate()` i przed zapisem child version, dla `ViolationDetail.rule == "THIRD-CONSECUTIVE-SHIFT-01"` użyć pełnych `assignment_ids` i zamrozić wyłącznie przecięcie z Assignmentami faktycznie edytowanymi w tej korekcie (`upsert_assignments`). Nie zamrażać całego okna, nie używać `Deviation.affected_assignment_or_employee`, nie rozszerzać ogólnej definicji redistributable na inne Deviation. Action trail/after-state musi pokazywać rzeczywiście zapisany `frozen=True`. R6 repro ma stać się testem regresyjnym: późniejszy Przelicz Plan nie może cofnąć ręcznie ustawionego przyszłego Assignmentu, który świadomie uczestniczy w nowym HARD. Status BOARD pozostaje formalnie `OWNER_DECISION_NEEDED` tylko dlatego, że nie ma osobnego `READY_FOR_CC`; decyzja architekta jest wydana i nie blokuje implementacji. Po poprawce + targetowanych testach CC ma ustawić `READY_FOR_CODEX` z exact SHA. |
| ROTA-T059 | Architekt | Codex | `task/ROTA-T059` | brief `d04bb65` | READY_FOR_CODEX | **PREIMPLEMENTATION solver-engineering — IMPLEMENTATION HOLD.** Cel: ownerowe 24h SOFT tolerance equity bez regresji CP-SAT i bez dalszego dokładania kruchych kaskadowych wag. Evidence: `arch/FINDING_2026-09-08_T058_EQUITY_DEADBAND_CPSAT_PERFORMANCE.md`. Codex ma porównać na tych samych realnych fixture co najmniej: (A) bezpieczniejszy bounded weighted objective, (B) sekwencyjną/lexicographic optymalizację z odtworzeniem dlaczego wcześniejszy phase split odrzucono i czy to nadal aktualne po T041/T057, (C) hierarchię jakości bez wielkich współczynników. Obowiązkowo complete-target, missing-target equal-split, ciasny staffing, PLAN, Przelicz Plan oraz fixture gdzie deadband faktycznie zmienia ranking. Zapisać wall time, gap, TARGET-01 quality, equity spread, rhythm score, HARD i deadband. Nie implementować produkcyjnie ani nie zwiększać budżetu 45s jako obejścia. Literalny TASK_SCOPE dopiero po wyborze architektury. |
| ROTA-CORRECTION-EFFECTIVE-FROM-DEFAULT | Codex | Architekt | — (pre-brief finding) | main@`1463188` | CODEX_REPORTED | **GOTOWE DO BRIEFU po skorygowaniu uzupełnienia CC.** OWNER_ACCEPTED 2026-09-05: przed rozpoczęciem pozostają zwykłe korekty planu z datą skutku wyliczoną z początku Assignmentu; po rozpoczęciu kod/godziny/demand/stan/freeze są niezmienne, a jedynym wyjątkiem jest zapisanie innego pracownika, który faktycznie wykonał całą służbę, z obowiązkową przyczyną i historią. Backend jest ownerem. **Potwierdzone ponownie 2026-09-06 (na wyraźną prośbę ownera, sprawdzone dla Korekty ręcznej po Plan/Replan/Przelicz Plan — patrz `ROTA-T057`):** `apply_manual_correction` (`manual_edit.py:297-346`) nie używa solvera w ogóle (nie ma tu problemu ginącej zmiennej decyzyjnej z T057), ale ma dokładnie tę samą lukę z innego powodu — jedyna kontrola to `validate_realized_preserved()`, działająca tylko dla stanu REALIZED, którego prawie nikt nie ustawia automatycznie; dla zwykłego już-odbytego, ale wciąż PLANNED przypisania nie ma żadnej kontroli czasowej, żadnego wymogu podania powodu, żadnego rozróżnienia dozwolonego wyjątku (inny pracownik faktycznie wykonał służbę) od nieautoryzowanej zmiany rdzenia służby. To nie nowy błąd, tylko potwierdzenie zakresu tego wiersza z realnego kodu, nie z domysłu. CC poprawnie znalazł `AssignmentState.REALIZED` i `validate_realized_preserved()` jako użyteczny bezpiecznik, ale nie jako kompletne rozwiązanie: stan nie jest całkiem nieużywany (obsługuje realizację szkolenia i jest czytany przez bilanse/historię świąt); zwykła korekta może dziś oznaczyć nawet przyszłe PRIMARY jako REALIZED; bezwzględna niezmienność zablokowałaby dozwoloną korektę faktycznego pracownika; restore starszej wersji omija blokadę przez utworzenie nowej gałęzi. Istniejący cutover chroni tylko wybór kandydata po REPLAN. Brief ma objąć wszystkie ścieżki, w tym restore i bezpośredni endpoint, oraz traktować REALIZED jako możliwy element, nie narzucone rozwiązanie. Pełny kontrakt zachowania: `arch/PREBRIEF_AUDIT_2026-09-05_T056_FOLLOWUPS.md`; niezależny reaudyt i reproduktory: `arch/PREBRIEF_REAUDIT_2026-09-05_HISTORICAL_SERVICE_REALIZED.md`. |
| ROTA-PRINT-IGNORES-UNACKED-DEVIATIONS | Codex | Architekt | — (pre-brief finding) | main@`e4d1504` | CODEX_REPORTED | **PROBLEM ZREDUKOWANY DO JEDNEJ CENTRALNEJ LUKI EKSPORTU — gotowy do briefu.** Korekta ręczna może utworzyć LAW, validator je zapisuje, ale endpoint PDF nie sprawdza świeżych LAW ani jednorazowego potwierdzenia i mimo to generuje wydruk. Przed każdym PDF backend ma świeżo sprawdzić dokładnie drukowany, wybrany grafik; niepotwierdzone LAW blokuje ten wydruk i wraca jako czytelna lista do potwierdzenia tylko dla tej jednej próby. Następny wydruk wymaga re-ack; ostrzeżenia nie-LAW nie blokują; bezpośredni POST nie omija bramki. Legalny wybrany `WORKING` nadal wolno drukować. Nie przedstawiać REPLAN/restore jako osobnych źródeł wadliwego grafiku ani osobnych napraw — ich cykl wersji został wydzielony do `ROTA-PLAN-VS-REPLAN-LIFECYCLE`. Starszy, szerszy materiał dowodowy: `arch/PREBRIEF_REAUDIT_2026-09-05_PRINT_LIFECYCLE_UI_IDENTIFIERS.md`. |
| ROTA-NO-PERSISTENT-ERROR-LOG | CC | Architekt | — (brak brancha, brak brief.md) | `2f10df3` (stan sprawdzony) | OWNER_DECISION_NEEDED | **Prośba o brief: program nie zapisuje błędów na bieżąco do żadnego pliku — sprawdzone wprost, nie z pamięci.** Sprawdzone: nie istnieje `logs/errors.log` ani żaden inny plik logu błędów aplikacji; brak jakiejkolwiek konfiguracji `logging.FileHandler`/`logging.basicConfig` w `api`/`rota`. Jedyny istniejący mechanizm zapisu (`log/session.txt` przez `session_log.py`) to dziennik pracy CC/Codexa nad zadaniami, nie log błędów działającego programu. Skutek: gdy koordynator trafia na błąd (np. surowy zrzut REPLAN na obiekcie Royal, 2026-09-06), jedyny ślad to to, co akurat zobaczy na ekranie i przekaże ręcznie — nie da się sprawdzić błędu po fakcie, brak czasu zdarzenia, brak pełnego stack trace. OWNER: to jest odpowiedź na wielokrotnie powtarzane pytanie "czy CC ma diagnostykę" — nie miał, dopiero żywy klik na Royal to ujawnił. CC nie projektuje rozwiązania (wybór: plik rotowany, poziom logowania, co dokładnie logować przy RODO wrażliwych danych pracowników) — to wymaga architekta. |
| ROTA-DEVIATION-RAW-ASSIGNMENT-ID | Codex | Architekt / CC | — (pre-brief finding, zależność PRINT-BLOCK) | main@`e4d1504` | CODEX_REPORTED | **OWNER_ACCEPTED 2026-09-05: wszystkie techniczne identyfikatory mają zniknąć z całego UI koordynatora.** CC nie wykonał jeszcze pełnego inventory. Reaudyt potwierdził dalsze powierzchnie: Overview drukuje employee_id; Decisions ma fallback ID/requested_by/linked action IDs/raw condition; History pokazuje coordinator/source/decision/action/rule IDs i ID w before/after; Export pokazuje revision hash, siteId w nazwie pliku i dwa kody hash na PDF; Analytics pokazuje siteId; wspólny klient renderuje surowe `body.detail`, a backend detail często zawiera wewnętrzne ID. To minimalne source inventory, nie zamknięcie runtime — CC/architekt ma dokończyć realne ścieżki wszystkich ekranów i jawnie podzielić Taski. „LAW Godziny” nadal nie wystarcza. Audyty: `arch/PREBRIEF_AUDIT_2026-09-05_T056_FOLLOWUPS.md` oraz `arch/PREBRIEF_REAUDIT_2026-09-05_HISTORICAL_SERVICE_REALIZED.md`. |
| ROTA-TEST-CLEANUP | CC | Codex | `task/ROTA-TEST-CLEANUP` | `dbc44ca` | READY_FOR_CODEX | **13 z 20 przedawnionych, niezwiązanych failujących testów naprawione (dopasowane do już zmergowanych T057/T041); 7 celowo NIE naprawionych — patrz niżej, każdy wymaga decyzji, nie mechanicznej łatki.** Naprawione: stała wersji schematu (`test_t012.py`); 7 testów REPLAN-po-akceptacji zaktualizowanych na Przelicz Plan (`test_t009_manual_edit.py`, `test_audit_t009_r4.py`×4, `test_audit_t009_r5.py`, symulator HTTP); 2 testy kalendarza/SICK_LEAVE w `test_t019.py` (T041 OWNER-T041-02 zastąpił starą regułę "SICK_LEAVE bez PRE_PLAN_LEAVE"); `test_t023_checkpoint_b.py` — zamrożony punkt odniesienia git-diff przesunięty naprzód (jak `guard.py freeze`, plik faktycznie zmieniał się w 3 późniejszych, legalnych taskach T023b/T032/T052) + druga część tego samego problemu kalendarza. **ZAFLAGOWANE (7 testów, nie naprawione):** (1) `test_t011_e...test_2/test_3` — T057 usunął tworzenie wersji roboczej przy DECISION_REQUIRED, więc mechanizm "Route A" z ROTA-T011-E (ręczna korekta rozwiązująca zablokowany plan) nie ma dziś czego poprawiać — wygląda na realną lukę wprowadzoną przez T057, nie przedawnienie testu. (2) `test_t019.py::test_11` + druga połowa `test_t23_34` — scenariusz "kwartał niedostępny przez niepełny kalendarz w innym miesiącu" stał się niekonstruowalny (zapis teraz pada szybko, nigdy nie dociera do odczytu) — oznaczone `@pytest.mark.skip` z pełnym uzasadnieniem, potrzebna decyzja: czy istnieje inna realna ścieżka do tego stanu, czy testy do usunięcia. (3) `test_replan_minimal_reshuffle.py::test_c_soft_ranking...` + `test_sick_leave.py::test_leave_granted...` — oba zakładają konkretny wynik tie-breaku equity przy remisie w sumie odchylenia od targetu (policzone ręcznie: remis w obu przypadkach), a solver wybiera przeciwieństwo tego, co testy zakładają — podejrzewany realny problem w formule equity, tematycznie blisko już otwartego `ROTA-SOLVER-RHYTHM-VS-TARGET`. (4) `test_t017.py::test_m13` — zamrożona dokładna wyrocznia CP-SAT na w pełni symetrycznym problemie, z natury krucha na dowolną nieistotną zmianę kolejności zmiennych, nie sygnał realnego błędu — kandydat do usunięcia/przeprojektowania. Wariant B (`test_coordinator_simulator_variant_b.py`, 2 testy) sprawdzony i świadomie pozostawiony po decyzji ownera ("logika zepsuta, nie ma sensu naprawiać") — potwierdzone głębsze niż nazewnictwo (nadal 409, teraz na innym endpoincie). Pełny `tests/`: 7 failed (dokładnie zestaw zaflagowany wyżej), 1372 passed, 2 skipped — zero nowych regresji. |