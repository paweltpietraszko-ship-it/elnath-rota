# BOARD.md — kolejka przekazań CC ↔ Codex

Nie czytane automatycznie jak AGENTS.md — trzeba wprost polecić "na początku
czytaj BOARD.md" (patrz AGENTS.md). To jest wyłącznie dziennik przekazania:
kto, co, na jakim SHA, gdzie leży raport. Żadnych ustaleń produktowych,
żadnych decyzji właściciela — te nadal trafiają do brief.md/kontraktu danego
Tasku. PR pozostaje realnym wyzwalzem pracy; ten plik tylko rejestruje
przekazanie, żeby nie trzeba było ręcznie przeklejać wiadomości między CC a
Codexem.

Statusy (dokładnie cztery, nic więcej):

- `READY_FOR_CODEX` — CC skończył, czeka na audyt.
- `CODEX_IN_PROGRESS` — Codex audytuje.
- `CODEX_REPORTED` — raport gotowy pod wskazaną ścieżką.
- `OWNER_DECISION_NEEDED` — audyt utknął na decyzji właściciela.

Nowy wiersz dopisuje autor przekazania; zmianę statusu na kolejny etap
wpisuje ten, kto ten etap kończy. Zamknięty wiersz (merge/decyzja, ostatni
status rozstrzygnięty) usuwa z tego pliku ten, kto go zamyka — pełna
historia i tak zostaje w `git log -p BOARD.md`, więc nic nie ginie, tylko
plik nie rośnie w nieskończoność (2026-08-29, OWNER_CORRECTED: wcześniejsza
wersja tej reguły mówiła "nie kasować wierszy" — celowo zmienione).

| ID | Autor | Odbiorca | Branch | Exact SHA | Status | Wiadomość |
|---|---|---|---|---|---|---|
| ROTA-CROSS-CONTEXT-vs-CORRECTION-EFFECTIVE-SCOPE | Codex | Architekt | — (pre-brief scope review) | main@`d402426` | CODEX_REPORTED | **NIE ŁĄCZYĆ W JEDEN TASK; realizować kolejno.** `ROTA-CROSS-CONTEXT-DEVIATION-TARGET` jest pilną, wąską naprawą crasha w referencyjnej walidacji celu `Deviation` (`schedule_validation.py::_validate_one_deviation`); nie wymaga zmiany zasad Korekty ręcznej i według findingu nie powinien dotykać `manual_edit.py`. `ROTA-CORRECTION-EFFECTIVE-FROM-DEFAULT` egzekwuje osobny, szerszy kontrakt właściciela dotyczący już rozpoczętych/odbytych służb: granicę czasu, dozwolony wyjątek podmiany faktycznego pracownika, obowiązkowy powód i historię oraz ochronę przez Korektę ręczną, endpoint, REPLAN/select i restore. Wspólne jest tylko miejsce ujawnienia problemu (scenariusz Korekty ręcznej), nie właściciel ani klasa naprawy. Połączenie opóźni usunięcie realnego crasha i utrudni niezależny audyt/rollback. Architekt może przygotować oba briefy podczas jednej sesji, lecz jako dwa odrębne kontrakty: najpierw cross-context crash, bezpośrednio po nim ochrona korekt historycznych. Evidence: wąskie `where.py` dla `_validate_one_deviation`, `validate_realized_preserved`, `apply_manual_correction`, `restore_schedule_version` + bezpośredni odczyt kodu na wskazanym main. |
| ROTA-T059 | Architekt | Codex | `task/ROTA-T059` | brief `d04bb65` | READY_FOR_CODEX | **PREIMPLEMENTATION solver-engineering — IMPLEMENTATION HOLD.** Cel: ownerowe 24h SOFT tolerance equity bez regresji CP-SAT i bez dalszego dokładania kruchych kaskadowych wag. Evidence: `arch/FINDING_2026-09-08_T058_EQUITY_DEADBAND_CPSAT_PERFORMANCE.md`. Codex ma porównać na tych samych realnych fixture co najmniej: (A) bezpieczniejszy bounded weighted objective, (B) sekwencyjną/lexicographic optymalizację z odtworzeniem dlaczego wcześniejszy phase split odrzucono i czy to nadal aktualne po T041/T057, (C) hierarchię jakości bez wielkich współczynników. Obowiązkowo complete-target, missing-target equal-split, ciasny staffing, PLAN, Przelicz Plan oraz fixture gdzie deadband faktycznie zmienia ranking. Zapisać wall time, gap, TARGET-01 quality, equity spread, rhythm score, HARD i deadband. **OWNER_RULING 2026-09-09:** istniejące `benchmarks/` i dwa testy benchmarków są zamrożone i T059 nie może ich użyć. Wymagane tutaj pomiary są wąskimi pomiarami należącymi do T059, wykonywanymi według jego zamrożonego kontraktu; nie stanowią odmrożenia starego zestawu. Nie implementować produkcyjnie ani nie zwiększać budżetu 45s jako obejścia. Literalny TASK_SCOPE dopiero po wyborze architektury. |
| ROTA-CORRECTION-EFFECTIVE-FROM-DEFAULT | Architekt | Codex | `task/ROTA-CORRECTION-EFFECTIVE-FROM-DEFAULT` | brief `d44275e` | READY_FOR_CODEX | **PREIMPLEMENTATION — IMPLEMENTATION HOLD.** Osobny kontrakt ochrony rozpoczętej/historycznej służby. Przed startem zwykłe korekty pozostają, ale backend wylicza `effective_from` z daty początku Assignmentu; po `start_datetime <= now` wszystkie fakty służby są niezmienne poza jednym świadomym wyjątkiem: zmiana wyłącznie `employee_id` na osobę, która faktycznie wykonała całą służbę, z obowiązkową przyczyną i istniejącą historią before/after. Reużyć istniejący cutover dla Przelicz Plan/REPLAN/select; restore ma odrzucić starszą wersję zmieniającą rozpoczęte fakty. Bez nowego rejestru wykonanej pracy, bez zmian solvera i bez automatycznego REALIZED dla PRIMARY. Codex ma odpowiedzieć tylko na 4 pytania z §11 i wskazać ewentualną konkretną brakującą ścieżkę; bez redesignu. |
| ROTA-PRINT-IGNORES-UNACKED-DEVIATIONS | Codex | Architekt | — (pre-brief finding) | main@`e4d1504` | CODEX_REPORTED | **PROBLEM ZREDUKOWANY DO JEDNEJ CENTRALNEJ LUKI EKSPORTU — gotowy do briefu.** Korekta ręczna może utworzyć LAW, validator je zapisuje, ale endpoint PDF nie sprawdza świeżych LAW ani jednorazowego potwierdzenia i mimo to generuje wydruk. Przed każdym PDF backend ma świeżo sprawdzić dokładnie drukowany, wybrany grafik; niepotwierdzone LAW blokuje ten wydruk i wraca jako czytelna lista do potwierdzenia tylko dla tej jednej próby. Następny wydruk wymaga re-ack; ostrzeżenia nie-LAW nie blokują; bezpośredni POST no bypass. Legalny wybrany `WORKING` nadal wolno drukować. Nie przedstawiać REPLAN/restore jako osobnych źródeł wadliwego grafiku ani osobnych napraw — ich cykl wersji został wydzielony do `ROTA-PLAN-VS-REPLAN-LIFECYCLE`. Starszy, szerszy materiał dowodowy: `arch/PREBRIEF_REAUDIT_2026-09-05_PRINT_LIFECYCLE_UI_IDENTIFIERS.md`. |
| ROTA-T061 | OWNER | Architekt / future instances | `task/ROTA-T061` | retired brief `218d0f5` | CODEX_REPORTED | **RETIRED / DO NOT IMPLEMENT — OWNER REJECTED 2026-09-10.** Nie wracać do T061 bez nowej jawnej decyzji OWNERA. Powód: proponowane `first manual root without current` wymagałoby zbudowania pełnego ręcznego układania miesiąca, czyli nowej funkcji/programu, a nie korekty istniejącego grafiku. Finalna granica produktu: pierwszy grafik powstaje wyłącznie przez `PLAN`; istniejąca `Korekta ręczna` dotyczy tylko grafiku, który już istnieje. Jeśli po dopuszczalnych zmianach obsady/dostępności/urlopów nadal nie da się ułożyć grafiku, **brak grafiku jest poprawnym wynikiem biznesowym**; nie tworzyć `current`, manual root ani obejścia. `test_t011_e...::test_2`/`::test_3` nie mogą wymuszać odrzuconego zachowania — test nie tworzy kontraktu. Stare briefy, raporty/PASS i propozycje implementacji T061 są obsolete. Następca produktowy: **ROTA-T062**. |
| ROTA-TECHNICAL-ERROR-RECOVERY-UX | Codex / OWNER | Architekt | — (pre-brief finding) | main current | CODEX_REPORTED | **OSOBNY TEMAT — nie rozszerza T061 ani T062.** OWNER_CONFIRMED: `TECHNICAL_ERROR`, HTTP error, timeout, zawieszenie/utrata backendu lub inna awaria wykonania oznaczają problem techniczny, nie blocker biznesowy i nie są podstawą do proponowania Korekty ręcznej. Program nie może jednak zostawić koordynatora ze ścianą „Wystąpił nieoczekiwany błąd. Spróbuj ponownie”. Komunikat ma prostym językiem powiedzieć, że wystąpiła awaria techniczna, oraz wskazać realne następne działanie: restart programu albo kontakt ze wsparciem. Nie pokazywać surowych statusów, kodów HTTP, stack trace ani technicznych identyfikatorów. Architekt ma ustalić osobny wąski kontrakt: gdzie komunikat obowiązuje, co dokładnie oznacza restart w tej aplikacji, jak użytkownik kontaktuje się ze wsparciem i jakie bezpieczne dane diagnostyczne może przekazać; powiązać z istniejącym findingiem `ROTA-NO-PERSISTENT-ERROR-LOG`, ale nie łączyć automatycznie zakresów bez decyzji OWNERA. |
| ROTA-RODO-ENCRYPTION-AT-REST | CC | Architekt | — (brak brancha, brak brief.md) | `2f10df3` (stan sprawdzony) | OWNER_DECISION_NEEDED | **Prośba o brief: baza SQLite jest dziś nieszyfrowana, i dane osobowe nie są tylko w jednym miejscu — sprawdzone wprost, nie z domysłu.** Kontekst: `arch/FINDING_2026-08-23_RODO_INTERNAL_NETWORK_BASELINE.md` już flagował "encryption at rest (SQLite store currently unencrypted)" jako art. 32 do rozważenia — to prośba ownera (2026-09-09), żeby to faktycznie ruszyć, nie zostawiać w dokumencie. Sprawdzone teraz: (1) główny magazyn to zwykły, nieszyfrowany plik SQLite (`rota/persistence/db.py::sqlite3.connect`). (2) `/workspace/backup` (`api/routers/backup.py`) kopiuje CAŁĄ bazę do pliku tymczasowego systemu operacyjnego (`tempfile.mkstemp(suffix=".db")`), streamuje do przeglądarki, usuwa dopiero po wysłaniu odpowiedzi — czyli przez czas trwania requestu istnieje DRUGA, osobna nieszyfrowana kopia wszystkich danych osobowych na dysku. (3) Eksport PDF, diagnostyka (`/workspace/diagnostics`), frontendowy bufor diagnostyczny i `localStorage` sprawdzone i CZYSTE — żadne z nich nie niosą treści osobowych (PDF czysto w pamięci→base64, diagnostyka celowo ogranicza się do liczników wierszy/nazw komponentów, nigdy treści). Materiał porównawczy od ownera: dwa bliźniacze projekty na GitHub z realnym, działającym szyfrowaniem, ale różnymi stackami: `LynxMask-Desktop` (`C:\Users\p_pie\Desktop\LynxMask-Desktop`, Python) — AES-256-GCM + Windows-DPAPI keystore + per-entry HMAC-SHA256, `backend/anonymizer_crypto.py`; `LynxMask` mobile (`C:\Projects\LynxMask`, Kotlin/Android) — AndroidKeyStore AES-256-GCM + SQLCipher (osobne hasło do bazy), `app/src/main/java/com/lynxmask/app/SessionStore.kt`. Żaden z dwóch nie przenosi się wprost na serwerowy (Railway) kontekst Roty (DPAPI to Windows-only, AndroidKeyStore to Android-only) — ale SQLCipher jest wspólnym mianownikiem obu (mobile już go używa, desktop miał go jako realnego kandydata w tej samej wcześniejszej notatce). CC nie projektuje rozwiązania (wybór: SQLCipher dla całej bazy, szyfrowanie na poziomie pól reużywające tych samych prymitywów AES-GCM, czy szyfrowanie na poziomie dysku/OS; plus osobno trzeba zamknąć lukę z `/backup`'s tymczasowym plikiem niezależnie od wybranego podejścia) — to wymaga architekta. |
| ROTA-NO-PERSISTENT-ERROR-LOG | CC | Architekt | — (brak brancha, brak brief.md) | `2f10df3` (stan sprawdzony) | OWNER_DECISION_NEEDED | **Prośba o brief: program nie zapisuje błędów na bieżąco do żadnego pliku — sprawdzone wprost, nie z pamięci.** Sprawdzone: nie istnieje `logs/errors.log` ani żaden inny plik logu błędów aplikacji; brak jakiejkolwiek konfiguracji `logging.FileHandler`/`logging.basicConfig` w `api`/`rota`. Jedyny istniejący mechanizm zapisu (`log/session.txt` przez `session_log.py`) to dziennik pracy CC/Codexa nad zadaniami, nie log błędów działającego programu. Skutek: gdy koordynator trafia na błąd (np. surowy zrzut REPLAN na obiekcie Royal, 2026-09-06), jedyny ślad to to, co akurat zobaczy na ekranie i przekaże ręcznie — nie da się sprawdzić błędu po fakcie, brak czasu zdarzenia, brak pełnego stack trace. OWNER: to jest odpowiedź na wielokrotnie powtarzane pytanie "czy CC ma diagnostykę" — nie miał, dopiero żywy klik na Royal to ujawnił. CC nie projektuje rozwiązania (wybór: plik rotowany, poziom logowania, co dokładnie logować przy RODO wrażliwych danych pracowników) — to wymaga architekta. |
| ROTA-TEST-CLEANUP | CC | Codex | `task/ROTA-TEST-CLEANUP` | `2a8ca39` | READY_FOR_CODEX | **Aktualny stan po decyzji OWNERA o zamrożeniu Symulatorów A/B oraz starych benchmarków.** Symulatory (`tests/property/coordinator_simulator.py`, `test_coordinator_simulator.py`, `test_coordinator_simulator_variant_b.py`), cały katalog `benchmarks/` oraz `tests/test_rota_stress_benchmark.py` i `tests/test_real_object_benchmark.py` są poza audytem, regresją i licznikami; nie uruchamiać ich, nie poprawiać i nie traktować ich wyników jako blockerów. Najnowsza gałąź dodatkowo usuwa kruchy `test_m13`; niezależny wąski re-check `tests/test_t017.py`: 26/26 PASS. Po wyłączeniu zamrożonych narzędzi jedynymi potwierdzonymi nierozwiązanymi testami są `test_t011_e...::test_2` i `::test_3`: 2/2 FAIL na `NoCurrentScheduleVersion`, zgodnie z osobnym findingiem `ROTA-T057-ROUTE-A-REGRESSION`, który jest gotowy do briefu architekta. Nie podawać dawnego licznika „17/20”, bo miesza zamrożone narzędzia z aktywną macierzą. Cleanup nie potrzebuje osobnego briefu produktowego; Codex może audytować jego mechaniczne poprawki, ale merge pozostaje zależny od rozstrzygnięcia Route A albo jawnego wyłączenia tych dwóch testów do czasu wdrożenia osobnego Tasku. |
| ROTA-CROSS-CONTEXT-DEVIATION-TARGET | Architekt | Codex | `task/ROTA-CROSS-CONTEXT-DEVIATION-TARGET` | brief `6b9d64f` | READY_FOR_CODEX | **PREIMPLEMENTATION — IMPLEMENTATION HOLD; wykonać przed CORRECTION-EFFECTIVE.** Wąska naprawa crasha `MalformedScheduleSnapshot`: dla `DeviationCategory.LAW` target może dodatkowo wskazać Assignment należący do aktualnie bieżącej ScheduleVersion w cross-month/cross-site context. Existing Employee/same-version Assignment/COVERAGE ShiftDemand pozostają bez zmian; historyczna nie-current wersja nadal niedozwolona; inne kategorie bez nowej furtki. Literalny production scope: tylko `rota/persistence/schedule_validation.py`. Bez `manual_edit.py`, solvera, validatora planowania i bez zmiany semantyki REST/WEEKLY-REST. Codex ma odpowiedzieć tylko na 3 pytania z §7; jeśli 3x TAK, PASS exact SHA i zwolnić HOLD. |
| ROTA-T065 | CC | Architekt | — (finding, brak briefu) | `3d9600f` (finding) | OWNER_DECISION_NEEDED | **OWNER_CONFIRMED 2026-09-10 — NIE ODŁOŻONE: podjąć po ustabilizowaniu modułu Ochrona.** Pełny finding: `arch/FINDING_2026-09-03_STANDARDOWY_EMPLOYEE_CATEGORIES.md` (istniał lokalnie od 2026-09-03, dopiero teraz scommitowany — architekt nigdy wcześniej go nie widział). Sedno: `planning_regime=ORDINARY` ("Standardowy") to dziś dosłownie skopiowana logika Ochrony, bez własnej tożsamości. Realny cel: sklep żony Pawła — zmiany 5-12 i 12-19 pon-pt, 5-14 w sobotę, z TRZEMA niezależnie rotującymi warstwami załogi: kierownicy, załoga, uczniowie (czwarta, "ekipa sprzątająca", jawnie niepewna). Zweryfikowane dwuprzebiegowo (agent czytający pełne pliki + spot-check CC przez `where.py` i bezpośredni odczyt `fairness.py`/`constraints.py`): (1) dowolne godziny zmian per dzień tygodnia — JUŻ DZIAŁA, `StandardShift` ma wolne `start_time`/`end_time`, zero zmian kodu; (2) zamknięta niedziela — JUŻ DZIAŁA innym mechanizmem (brak dnia 7 w `active_weekdays`, nie przez `CalendarDay.holiday`); (3) niezależne, nie-zamienne kategorie pracowników — POTWIERDZONE BRAKUJĄCE, brak jakiegokolwiek częściowego mechanizmu w `rota/`. Mapa dotknięcia (faktyczna, nie projekt): `rota/domain.py` (wartość kategorii na `SiteMembership`), `rota/planning/shift_catalog.py` (przeniesienie kategorii z `StandardShift` do `ShiftDemand`), `rota/planning/eligibility.py` (nowa twarda brama kategorii, analogiczna do istniejących), `rota/planning/solver.py` (prawdopodobnie bez zmian strukturalnych — jedna brama eligibility może wystarczyć). Dodatkowo, z późniejszej uwagi Pawła: potrzebny NOWY WYDRUK z grupowaniem po kategorii (`schedule_export.py::_build_rows` dziś czysto alfabetyczne, jedna płaska tabela) — to osobny kawałek zakresu, nie automatycznie pokryty zmianą backendu. Skala: architektoniczna (dotyka modelu domeny, bramy eligibility i generowania katalogu — trzy pliki), ale węższa niż przebudowa solvera; nowe pola addytywne, wzorowane na istniejących (`can_work_24h`, `membership_kind`, `catalog_kind`). Otwarte, nierozstrzygnięte w findingu: czy kategoria żyje na `Employee` czy `SiteMembership`; czy "ekipa sprzątająca" wchodzi w zakres teraz; interakcja kategorii z `EXTERNAL_SUPPORT`/`can_work_24h`/`SiteRule`; dokładny układ wydruku; czy to jeden Task czy dwa (backend+solver vs. wydruk). CC nie projektuje rozwiązania — architekt przygotowuje brief, gdy Paweł da sygnał (po ustabilizowaniu Ochrony). |