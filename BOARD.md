# BOARD.md — kolejka przekazań CC ↔ Codex

Nie czytane automatycznie jak AGENTS.md — trzeba wprost polecić "na początku
czytaj BOARD.md" (patrz AGENTS.md). To jest wyłącznie dziennik przekazania:
kto, co, na jakim SHA, gdzie leży raport. Żadnych ustaleń produktowych,
żadnych decyzji właściciela — te nadal trafiają do brief.md/kontraktu danego
Tasku. PR pozostaje realnym wyzwalzem pracy; ten plik tylko rejestruje
przekazanie, żeby nie trzeba było ręcznie przeklejać wiadomości między CC a
Codexem.

Statusy (dokładnie cztery, nic więcej):

- `READY_FOR_CODEX` — CC skończył, czeka na audyt.
- `CODEX_IN_PROGRESS` — Codex audytuje.
- `CODEX_REPORTED` — raport gotowy pod wskazaną ścieżką.
- `OWNER_DECISION_NEEDED` — audyt utknął na decyzji właściciela.

Nowy wiersz dopisuje autor przekazania; zmianę statusu na kolejny etap
wpisuje ten, kto ten etap kończy. Zamknięty wiersz (merge/decyzja, ostatni
status rozstrzygnięty) usuwa z tego pliku ten, kto go zamyka — pełna
historia i tak zostaje w `git log -p BOARD.md`, więc nic nie ginie, tylko
plik nie rośnie w nieskończoność (2026-08-29, OWNER_CORRECTED: wcześniejsza
wersja tej reguły mówiła "nie kasować wierszy" — celowo zmienione).

| ID | Autor | Odbiorca | Branch | Exact SHA | Status | Wiadomość |
|---|---|---|---|---|---|---|
| ROTA-BACKEND-TASK-SCOPE-FORMAT-DRIFT | Codex | Architekt/CC | `task/ROTA-BACKEND-TASK-SCOPE-FORMAT-DRIFT` | audit `b8a2a7a` na corrected brief/tests `df2e859fb263c92d343791d1b9e2a57127b34dda` | CODEX_REPORTED | **PASS PREIMPLEMENTATION R3.** Korekta jest kompletna: invalid UTF-8 ma kontrolowany kontrakt CLI bez tracebacku/DIFF_SCOPE, osobny nagłówek Markdown zamyka scope, a zredukowane piony CLI zachowują pełne klasy w testach bezpośrednich. `backend.py` nadal niezmieniony. Baseline acceptance + audit repro: 26 PASS / 21 oczekiwanych FAIL ujawniających stan przed poprawką; ruff i diff-check PASS. Raport: `tasks/ROTA-BACKEND-TASK-SCOPE-FORMAT-DRIFT/round_01/tests/tests_r3.txt`. Implementacja może rozpocząć się wyłącznie według briefu `df2e859`; finalny audyt użyje `audit_r2_repro.py`, modułu acceptance i rzeczywistego CLI. |
| ROTA-T065-PRINT-GAP | Codex | Architekt/CC | `task/ROTA-T065-PRINT-GAP` | audit `eda3b0a` (brief B `8bf47a21484277ffecf0ae7a6223b28fe970c971`) | CODEX_REPORTED | **PASS — architektura CHECKPOINT B zamknięta.** Raport: `tasks/ROTA-T065-PRINT-GAP/round_01/tests/tests_r5.txt`. 11 decyzji jest w briefie jako PRODUCT_TRUTH; stary kontrakt roli przy godzinach usunięto; jedna historyczna etykieta stanowiska ma jawnego ownera/dependency w `ROTA-T065-CONFIGURABLE-ROLES`; PRINT-GAP nie tworzy modelu ról. Jeden export lifecycle i scope pozostają zachowane. Implementacja produkcyjna nadal czeka wyłącznie na techniczną dostępność historycznego stanowiska z dependency. Nie powtarzano audytu prototypu ani nie uruchamiano testów produktu/regresji. |
| ROTA-T065-MANUAL-MIDDLE-SHIFT | Codex | CC | `task/ROTA-T065-MANUAL-MIDDLE-SHIFT` | audit `78b7255` (brief `8ab0d94ca40fc99b10dfc1eecc3581cadee1fa8b`) | CODEX_REPORTED | **PASS PREIMPLEMENTATION; IMPLEMENTATION HOLD DO ZALEŻNOŚCI.** Raport `tasks/ROTA-T065-MANUAL-MIDDLE-SHIFT/round_01/tests/tests_r2.txt`. Fail-closed marker legalnego PRIMARY bez demandu, jeden RoleCoverageAuthorization, wspólny hourly oracle, normalne HARD-y bez S1 i zgodność z PDF są zamknięte. Implementować dopiero po produkcyjnym domknięciu CONFIGURABLE-ROLES i ORDINARY-TIME-AVAILABILITY. |
| ROTA-EMPLOYEE-DETAIL-SIZE-LIMIT | CC | Architekt | — (finding, brak briefu) | `frontend/src/screens/EmployeeDetail.tsx` @ `task/ROTA-T065-ORDINARY-TIME-AVAILABILITY@9f3c86d`, 1055 linii | OWNER_DECISION_NEEDED | **Plik przekracza limit 600 linii (był 988 na `main` już przed dzisiejszą sesją, teraz 1055).** Wielokrotne kolejne Taski (T021 audyty, T053, T062, T064, CONFIGURABLE-ROLES, teraz ORDINARY-TIME-AVAILABILITY) dokładały tu funkcjonalność wymienioną wprost w swoich WHERE_MAP, każdy przyrost osobno mały i uzasadniony, ale plik nigdy nie był dzielony. Paweł zauważył to w trakcie dzisiejszej sesji przy ORDINARY-TIME-AVAILABILITY; owner decision: dokończyć bieżący brief minimalnie (zrobione), zgłosić przekroczenie osobno zamiast łączyć z bieżącym Taskiem. Potrzebna decyzja architekta/właściciela: osobny Task refaktoryzujący podział na mniejsze komponenty (np. macierz dostępności, formularz nieobecności, log historii jako osobne pliki), zanim kolejny Task znowu tu doda funkcjonalność. |
| ROTA-FILES-REFACTOR-L1 | CC | Codex | `task/ROTA-FILES-REFACTOR-L1` | impl `8a9a6df` (base `bad290f`, brief `tasks/ROTA-FILES-REFACTOR-L1/brief.md`) | READY_FOR_CODEX | **Poziom 1 refaktoru rozmiaru plików (owner-authorized w czacie, brak briefu architekta).** Podział `rota/planning/validator.py` (988 linii) na `validator_shared.py` + trzy pliki `validator_checks_*.py` pogrupowane tematycznie + cienki orchestrator `validator.py` (109 linii), wszystkie <600. Czysty przenoszenie kodu — każda funkcja `_check_*` bez zmian, tylko lokalizacja pliku; `validate()` woła je w dokładnie tym samym porządku. Zachowany kontrakt `tests/test_t023.py::test_t23_55_...` (literalna ścieżka importu `coverage_segments` z `rota.planning.validator`) przez re-export. Ruff czysty na wszystkich 6 plikach. Uruchomione wszystkie 28 plików testowych importujących cokolwiek z `rota.planning.validator`: 23 fail / 542 pass, wszystkie 23 niezależnie potwierdzone identyczne na czystym worktree `origin/main` — zero nowych regresji od podziału. `backend.py` celowo nie uruchamiany (bramka jest przedmiotem osobnego `ROTA-BACKEND-TASK-SCOPE-FORMAT-DRIFT`) — owner świadomie zaakceptował brak mechanicznego backupu dla tego jednego Taska. Brief dokumentuje zakres/acceptance dla audytu. |
| ROTA-NEWLY-FOUND-PREEXISTING-FAILURES | CC | Architekt | — (finding, brak briefu) | `main@bad290f`, znaleziono przy weryfikacji `ROTA-FILES-REFACTOR-L1` | OWNER_DECISION_NEEDED | **5 dodatkowych pre-existing failurów, wcześniej niekatalogowanych, niezwiązanych z refaktorem `validator.py`.** Trzy grupy przyczyn: (1) `test_audit_r25_findings.py::test_r25_1a_...` i `test_t017.py::test_m29_...` — ten sam wzorzec co wcześniej dziś naprawiany wielokrotnie: fixture konstruuje ORDINARY membership bez roli, trafia w inwariant `_require_ordinary_position_still_valid` z CONFIGURABLE-ROLES ("aktywny pracownik obiektu standardowego musi mieć przypisane stanowisko") — mechaniczny fallout, ten sam fix co poprzednio (przełączyć fixture na OCHRONA albo dodać rolę). (2) `test_t023b.py::test_t23b_01_...` — stały literał `LATEST_SCHEMA_VERSION == 20`, nieaktualizowany po migracji 21 (ORDINARY-TIME-AVAILABILITY) — ten sam wzorzec co wcześniejsze poprawki w `test_t019b.py`/`test_t020.py`. (3) `test_site_rules_execution.py::test_o_site_rules_module_has_no_persistence_or_sqlite_coupling` — fałszywy alarm: test skanuje surowy tekst źródła (nie AST) szukając podciągu `"rota.persistence"`; złapał wzmiankę w docstringu `rota/planning/availability.py` tłumaczącym gdzie leży write boundary, nie prawdziwy import. (4) `test_rota_generic_month.py::test_generic_month_respects_day_only_and_mentor_rule` — INNA przyczyna, realna asercja o zachowaniu solvera (assignment pracownika R o 18:00 wbrew regule day_only/mentor), niezbadana, wymaga osobnego dochodzenia. Wszystkie 5 potwierdzone identyczne na czystym `origin/main` — nie są winą dzisiejszego refaktoru. Nie naprawiane teraz — poza zakresem `ROTA-FILES-REFACTOR-L1`. |
