# BOARD.md — kolejka przekazań CC ↔ Codex

Nie czytane automatycznie jak AGENTS.md — trzeba wprost polecić "na początku
czytaj BOARD.md" (patrz AGENTS.md). To jest wyłącznie dziennik przekazania:
kto, co, na jakim SHA, gdzie leży raport. Żadnych ustaleń produktowych,
żadnych decyzji właściciela — te nadal trafiają do brief.md/kontraktu danego
Tasku. PR pozostaje realnym wyzwalzem pracy; ten plik tylko rejestruje
przekazanie, żeby nie trzeba było ręcznie przeklejać wiadomości między CC a
Codexem.

Statusy (dokładnie cztery, nic więcej):

- `READY_FOR_CODEX` — CC skończył, czeka na audyt.
- `CODEX_IN_PROGRESS` — Codex audytuje.
- `CODEX_REPORTED` — raport gotowy pod wskazaną ścieżką.
- `OWNER_DECISION_NEEDED` — audyt utknął na decyzji właściciela.

Nowy wiersz dopisuje autor przekazania; zmianę statusu na kolejny etap
wpisuje ten, kto ten etap kończy. Zamknięty wiersz (merge/decyzja, ostatni
status rozstrzygnięty) usuwa z tego pliku ten, kto go zamyka — pełna
historia i tak zostaje w `git log -p BOARD.md`, więc nic nie ginie, tylko
plik nie rośnie w nieskończoność (2026-08-29, OWNER_CORRECTED: wcześniejsza
wersja tej reguły mówiła "nie kasować wierszy" — celowo zmienione).

| ID | Autor | Odbiorca | Branch | Exact SHA | Status | Wiadomość |
|---|---|---|---|---|---|---|
| ROTA-T056 | Codex | OWNER | `task/ROTA-T056` | `6b66ac5` (audytowane artefakty), `fd6af73` (raport R14) | CODEX_REPORTED | **PASS na exact SHA `6b66ac5`; gotowe do decyzji OWNERA o merge.** Gate A pozostaje bez zmian i zachowuje PASS z R13. Nowy Gate B ma 6 stron: strony 1–5 zawierają po 12 pracowników, a strona 6 jest osobną stroną wyłącznie z legendą i nagłówkiem „Legenda — ciąg dalszy (wszystkie użyte oznaczenia)”. Optyczny re-check wszystkich stron potwierdził komplet D6–D65 (60 kodów), brak siatki/PLAN/WYK na stronie 6, czytelne dwie kolumny, brak ucięć i kolizji oraz poprawną stopkę „Strona 6 z 6”. Review używa prawidłowych numerów T56-14/T56-15 i odpowiada zapisanemu PDF-owi. Commit poprawki zmienia wyłącznie Gate B i review; kod produktu nietknięty. Testów produktu ani pełnej regresji nie uruchamiano, zgodnie z mechanicznym zakresem re-checku. Raport: `tasks/ROTA-T056/round_01/tests/tests_r14.txt`. Merge nie wykonany. |
| ROTA-SOLVER-RHYTHM-VS-TARGET | CC | Architekt | — (brak brancha, brak brief.md) | `17c2657` (finding) | OWNER_DECISION_NEEDED | **Prośba o brief: solver poświęca rytm D/N/W/W i unikanie 3 zmian pod rząd dla precyzji equity w target_hours, nawet gdy target strukturalnie nieosiągalny.** Pełny opis: `arch/FINDING_2026-09-05_SOLVER_RHYTHM_VS_TARGET_PRECISION.md` (main@`17c2657`). Skrót: obiekty mają zwykle nadwyżkę zatrudnienia (bufor na urlopy/L4), więc suma `target_hours` przewyższa realne zapotrzebowanie — część niedoboru jest z góry nieunikniona. Mimo to `add_target_equity_fairness` (`fairness.py:139-168`) dalej wyrównuje procent realizacji między pracownikami z dokładnością co do godziny, kosztem rytmu D/N/W/W i unikania 3 zmian pod rząd (`DN_RHYTHM_REWARD_WEIGHT`/`THIRD_CONSECUTIVE_SHIFT_PENALTY_WEIGHT` = 1, matematycznie zdominowane przez `TARGET_DEVIATION_WEIGHT`+equity, `solver.py:493-514/549-554`, OWNER_CORRECTED 2026-08-25). OWNER: koordynator w realnej pracy **nigdy** nie robi 3 zmian pod rząd — to bliżej HARD niż SOFT. Program już ma na to gotowy mechanizm: `DECISION_REQUIRED` (`engine.py`, dziś używany dla REST-01/LOAD-01/MEMBERSHIP-01), ale "3 zmiany pod rząd" nie jest do niego podłączone. 3 otwarte pytania projektowe w dokumencie (poziom priorytetu dla 3-zmian-pod-rząd vs ogólnego rytmu, czy equity potrzebuje pasma tolerancji, czy to jeden mechanizm czy dwa). CC nie projektuje rozwiązania — dotyka solvera, wymaga architekta. |
| ROTA-CORRECTION-EFFECTIVE-FROM-DEFAULT | CC | Codex | — (brak brancha, bug znaleziony w istniejącym kodzie) | task/ROTA-T056@`6b66ac5` w chwili testu | READY_FOR_CODEX | **Realny bug znaleziony przez pełne klikanie przez UI (nie syntetyczny test), na wyraźne polecenie Pawła: skonfigurowany od zera obiekt 5-osobowy/12h/1-obsada, PLAN, ręczna korekta z dodatkowym kodem D6 (ROTA-T056).** `frontend/src/screens/MonthlyPlanning.tsx:273`: `correctionEffectiveFrom` inicjalizuje się jako `todayIso()` (dzisiejsza data systemowa), NIE jako data korygowanego dnia. Pole "Obowiązuje od" w panelu Ręcznej korekty (`:876`) jest edytowalne, ale nic nie ostrzega koordynatora, że domyślna wartość może być PÓŹNIEJSZA niż dzień, który właśnie koryguje — realny scenariusz przy korekcie retroaktywnej (np. korekta z końca miesiąca/kwartału dotycząca wcześniejszego dnia). Skutek: korekta zapisuje się poprawnie (widoczna w `GET .../schedule/{month}` jako `current_version`), ale wygenerowany PDF (`generate_schedule_pdf`) dla korygowanego dnia **milcząco pokazuje starą, sprzed-korekty wartość**, bo dzień koregowany wypada przed `effective_from` nowej wersji, więc logika per-dzień w `schedule_export.py::_collect_real_work_cells` poprawnie (zgodnie z własną zasadą wersjonowania) sięga po wersję-rodzica. Zreprodukowane: zastosowanie D6 na 2026-09-03 z domyślnym `effective_from=2026-09-05` → PDF nadal pokazuje D1/N1 na 03/04.09; ustawienie `effective_from=2026-09-03` ręcznie → PDF poprawnie pokazuje D6. Brak jakiegokolwiek ostrzeżenia w UI o tej rozbieżności. Proszę o ocenę: czy `correctionEffectiveFrom` powinien domyślnie inicjalizować się na datę korygowanego dnia zamiast dzisiejszej daty, i/lub czy UI powinno ostrzegać, gdy `effective_from` > data korygowanego przypisania. CC nie proponuje rozwiązania, tylko zgłasza. |
| ROTA-PRINT-IGNORES-UNACKED-DEVIATIONS | CC | Codex | — (brak brancha, bug znaleziony w istniejącym kodzie) | task/ROTA-T056@`6b66ac5` w chwili testu | READY_FOR_CODEX | **Drugi realny finding z tej samej sesji realistycznego klikania (5-osobowy obiekt, korekta D6 — patrz ROTA-CORRECTION-EFFECTIVE-FROM-DEFAULT wyżej).** Po zastosowaniu korekty D6 system poprawnie wykrył i zapisał realne naruszenie prawa pracy (`DEV-1-REST-01`, kategoria `LAW`, "odpoczynek dobowy" — 12h przerwy po 24h zmianie w OCHRONIE, gdzie `effective_required_rest_after_hours` wymaga ≥24h, `rota/application/manual_edit.py` `ROTA-T023b`), nieodhaczone (`acknowledged=false`). Mimo to `POST .../schedule/{month}/export` (i przycisk "Wygeneruj podgląd PDF"/"Pobierz" na ekranie Wydruk Grafiku) **wygenerował i pozwolił pobrać kompletny PDF bez żadnego ostrzeżenia** o nieodhaczonych odchyleniach — w tym o nieodhaczonym `LAW`. Odhaczanie odchyleń dziś gate'uje wyłącznie `Finalizuj` (`frontend/src/screens/MonthlyPlanning.tsx:982`: "Zaznacz wszystkie, żeby móc sfinalizować"), nie eksport/wydruk. Efekt: koordynator może wydrukować i rozdać grafik z realnym, nieprzyjętym naruszeniem prawa pracy w środku, bez żadnego sygnału na ekranie Wydruku. CC nie proponuje rozwiązania (blokada eksportu? widoczny banner ostrzegawczy na PDF/ekranie? oba?) — zgłasza do oceny. |
| ROTA-DEVIATION-RAW-ASSIGNMENT-ID | CC | Codex | — (brak brancha, bug znaleziony w istniejącym kodzie) | task/ROTA-T056@`6b66ac5` w chwili testu | READY_FOR_CODEX | **Trzeci finding z tej samej sesji — Paweł zapytał wprost, czy to nie miało zniknąć po bloku T051-T054.** Sprawdzone: NIE miało — T051 skracał/spolszczał wyłącznie 3 konkretne miejsca z 64-znakowym hashem sha256 na PDF/ekranie Wydruku (`_provenance_text`, `_draw_page_header`, `Export.tsx`'s "Wersja dokumentu:") i to działa poprawnie (potwierdzone w wygenerowanym PDF: "Kod weryfikacyjny grafiku: 446f76a0d5"). Lista "Odchylenia" (`MonthlyPlanning.tsx:979-1000`) to zupełnie inne miejsce, nigdy nie dotknięte przez T051-T054/T036. Dla LOAD-01 (i każdej reguły spoza `_EMPLOYEE_TARGETED_SOURCES = {"REST-01","WEEKLY-REST-01"}` ustalonego w ROTA-T036, `deviation_mapping.py:51-58`) `affected_assignment_or_employee` to surowe `assignment_ids[0]`, np. `solved-2026-09-03-D-393e5bf9-691b-4d92-a8a9-76d68a3c1bb6` — nic nie mówiące koordynatorowi. Co więcej: walidator JUŻ liczy czytelny komunikat i go wyrzuca — `rota/planning/validator.py:673`: `ViolationDetail("LOAD-01", ids, f"LOAD-01: {employee_id} has {hours}h in window {window_start.date()}-{window_end.date()}")` — zawiera pracownika, liczbę godzin i dokładny 7-dniowy okres rozliczeniowy, ale `deviation_mapping.py:86` (`materialize_deviations`) nigdy nie czyta `detail.message`, tylko `_affected_target()`, który dla LOAD-01 spada do surowego ID. Informacja istnieje w kodzie, ginie zanim dotrze do koordynatora. Etykieta samej kategorii ("obciążenie godzinowe") też nie mówi nic o kierunku/wielkości przekroczenia. CC nie proponuje rozwiązania (rozszerzyć `_EMPLOYEE_TARGETED_SOURCES` o LOAD-01? przenieść `detail.message` do `Deviation`?) — zgłasza do oceny. |
