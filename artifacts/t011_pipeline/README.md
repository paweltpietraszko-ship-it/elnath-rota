# T011 integrated pipeline artifacts

Generated by:

`python -m benchmarks.t011_pipeline_audit --output-dir artifacts/t011_pipeline`

Files:

- `normal_month.svg` / `.csv` / `.json` — full October 2026 with a 14-day
  approved leave, a 5-day sick leave, and three DAY_SHIFT_OFF requests;
- `boundary_month.svg` / `.csv` / `.json` — the same month plus a second,
  7-day sick leave and a dated day-only N exception; the FINAL schedule uses
  the exception for Filip's N shift on 16 October;
- `coordinator_wall.json` — deliberate DECISION_REQUIRED scenario.  The N
  shift starts on 13 October and enters the first sick day on the 14th.  An N
  exception effective only from the 14th is not yet applicable to that demand.

Legend: `D` day shift, `N` night shift, `U` approved leave, `L4` sick leave,
`D-OFF` no day shift (night remains available).  These files are audit
evidence, not the product's future spreadsheet export.
