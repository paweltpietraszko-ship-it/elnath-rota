FILE: tests/property/coordinator_simulator.py
LAYER: tests
WARNING: working tree differs from HEAD for this file — symbols/imports below are read from the WORKING TREE, but reference search always targets HEAD. Commit or stash for a consistent view.
IMPORTS (8):
  __future__
  random
  dataclasses
  datetime
  math
  fastapi.testclient
  rota.domain
  rota.planning.shift_catalog

TOP_LEVEL_SYMBOLS (71):
  _row_hours
  monthly_hours_for_shape
  ObjectSpec
  _days_in_month
  _month_dates
  nominal_monthly_hours_kp
  random_object_spec
  AbsenceDraw
  random_absence_set
  local_employee_id
  seed_calendar
  _create_site
  _put_shift_catalog
  add_local_employee
  _add_local_roster
  declared_roster
  build_object
  apply_absences
  external_support_employee_id
  external_support_reaction
  run_plan
  run_replan
  select_first_candidate
  get_analytics
  get_month_view
  quarter_object_spec
  run_quarter
  _duration_hours
  _is_active_primary
  demand_kind_by_id
  actual_hours_by_employee
  weekend_hours_by_employee
  holiday_hours_by_employee
  _employee_day_kinds
  count_dn_rhythm_windows
  count_bad_third_windows
  count_soft_absence_collisions
  FairnessFacts
  compute_fairness_facts
  is_symmetric_control_object
  evaluate_fairness
  select_first_candidate_reporting_validate
  _actual_primary_hours_by_employee
  compute_quarter_oracle
  DemandAtomB
  _weekday_occurrences_b
  layer_hours_b
  layered_headcount_b
  has_any_coverage_b
  _dn_12h_atoms_b
  ObjectSpecB
  random_atoms_b
  random_object_spec_b
  catalog_rows_from_atoms_b
  local_employee_id_b
  declared_roster_b
  _create_site_b
  _put_shift_catalog_b
  add_local_employee_b
  build_object_b
  _workday_count_in_range
  _next_monday_on_or_after
  urlop_blocks_b
  sick_leave_draw_b
  initial_absences_b
  apply_absences_b
  external_support_employee_id_b
  external_support_reaction_b
  run_with_external_loop_b
  assert_headcount_b
  assert_closed_world_b

SYMBOL: _row_hours
  TOTAL_RAW_HITS: 2
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:118: def _row_hours(start: time, end: time) -> float:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:132: total += _row_hours(start, end) * required * layer_count
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: monthly_hours_for_shape
  TOTAL_RAW_HITS: 8
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T039/brief.md:38: wprost z rzeczywistych wierszy katalogu (`monthly_hours_for_shape`), nie z
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T039/brief.md:121: - Zweryfikowane osobno (bez pełnego przebiegu): `monthly_hours_for_shape`
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:122: def monthly_hours_for_shape(shift_shape: str, month: date, layer_count: int = 1) -> int:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:211: monthly_hours_needed = monthly_hours_for_shape(shift_shape, month, layer_count)
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:536: monthly_hours_needed=monthly_hours_for_shape("D_N_12H", month, 1),
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:60: hours_one_layer = sim.monthly_hours_for_shape(shift_shape, MONTH, 1)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:61: hours_this_layer_count = sim.monthly_hours_for_shape(shift_shape, MONTH, layer_count)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:427: layer_count=1, monthly_hours_needed=sim.monthly_hours_for_shape("SINGLE_24H", MONTH, 1),
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: ObjectSpec
  TOTAL_RAW_HITS: 32
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T038/round_01/tests/tests_r1.txt:22: - tests/property/coordinator_simulator.py sam tworzy ObjectSpec i obsadę,
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T038/round_01/tests/tests_r1.txt:68: - coordinator_simulator.py:105 losuje external_count w ObjectSpec;
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T038/round_01/tests/tests_r1.txt:91: :memory:, bez solvera: ObjectSpec -> build_object -> roster/target readback.
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T038/round_01/tests/tests_r2.txt:81: SPEC ObjectSpec(seed=0, month=2026-09-01, monthly_hours_needed=720,
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T044/brief.md:283: wzorów asymetrycznych), wyniki SUMOWANE. `month` (pole `ObjectSpec`) nadal
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T044/brief.md:289: # `month` to pole ObjectSpec, losowane jeden z 12 miesięcy 2026 -- używane
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T044/brief.md:365: **Miesiąc (`month`, pole `ObjectSpec`) jest losowany, jeden z 12 miesięcy
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:91: # _put_shift_catalog multiplies it by ObjectSpec.layer_count for a second
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:137: class ObjectSpec:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:189: def random_object_spec(seed: int, month: date) -> ObjectSpec:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:228: return ObjectSpec(
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:262: spec: ObjectSpec,
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:317: def local_employee_id(spec: ObjectSpec, index: int) -> str:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:334: def _create_site(client: TestClient, spec: ObjectSpec) -> str:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:346: def _put_shift_catalog(client: TestClient, site_id: str, spec: ObjectSpec) -> None:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:358: def add_local_employee(client: TestClient, site_id: str, spec: ObjectSpec, index: int, day_only: bool) -> str:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:378: def _add_local_roster(client: TestClient, site_id: str, spec: ObjectSpec) -> None:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:383: def declared_roster(spec: ObjectSpec) -> list[str]:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:389: def build_object(client: TestClient, spec: ObjectSpec) -> str:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:400: def apply_absences(client: TestClient, site_id: str, spec: ObjectSpec, draws: list[AbsenceDraw]) -> None:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:424: def external_support_employee_id(spec: ObjectSpec) -> str:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:428: def external_support_reaction(client: TestClient, site_id: str, month: date, spec: ObjectSpec) -> dict:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:528: def quarter_object_spec(seed: int, month: date) -> ObjectSpec:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:533: return ObjectSpec(
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:727: def count_soft_absence_collisions(assignments: list[dict], absence_draws: list[AbsenceDraw], spec: ObjectSpec) -> int:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:770: analytics_rows: list[dict], absence_draws: list[AbsenceDraw], spec: ObjectSpec,
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:812: def is_symmetric_control_object(spec: ObjectSpec, absence_draws: list[AbsenceDraw]) -> bool:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:821: *, spec: ObjectSpec, absence_draws: list[AbsenceDraw], has_external: bool,
    TEXT OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:64: # actual hour total -- proven by constructing an ObjectSpec directly
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:67: spec = sim.ObjectSpec(
    TEXT OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:422: directly-constructed, zero-absence ObjectSpec to actually land in the
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:425: spec = sim.ObjectSpec(
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: _days_in_month
  TOTAL_RAW_HITS: 3
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T044/round_01/tests/tests_r4.txt:37: python -c "from datetime import date; from math import ceil; from tests.property.coordinator_simulator import nominal_monthly_hours_kp,POLISH_2026_HOLIDAYS,_days_in_month; print([(m, _days_in_month(date(2026,m,1)), nominal_monthly_hours_kp(date(2026,m,1),POLISH_2026_HOLIDAYS), ceil(_days_in_month(date(2026,m,1))*24/(nominal_monthly_hours_kp(date(2026,m,1),POLISH_2026_HOLIDAYS)-24)), ceil(_days_in_month(date(2026,m,1))*48/(nominal_monthly_hours_kp(date(2026,m,1),POLISH_2026_HOLIDAYS)-24))) for m in range(1,13)])"
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:154: def _days_in_month(month: date) -> int:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:160: count = _days_in_month(month)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: _month_dates
  TOTAL_RAW_HITS: 9
  WARNING: name defined in 2 different files — hits below may belong to a different symbol of the same name:
    DEF SITE: rota/persistence/site_rule_assembly.py
    DEF SITE: tests/property/coordinator_simulator.py
    DEF  OTHER_FILE PRODUCTION     rota/persistence/site_rule_assembly.py:42: def _month_dates(month: date) -> list[date]:
    CALL OTHER_FILE PRODUCTION     rota/persistence/site_rule_assembly.py:68: for day in _month_dates(month):
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:129: for day in _month_dates(month):
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:159: def _month_dates(month: date) -> list[date]:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:172: days = _month_dates(month)
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:296: days = _month_dates(spec.month)
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:326: for day in _month_dates(month):
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:694: dates = _month_dates(month)
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:716: dates = _month_dates(month)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: nominal_monthly_hours_kp
  TOTAL_RAW_HITS: 21
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T038/brief.md:124: Naprawione: `nominal_monthly_hours_kp(month)` — realny wzór art. 130 KP
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T038/round_01/tests/tests_r3.txt:25: - nominal_monthly_hours_kp(2026-09-01) = 176;
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T043/round_01/tests/tests_r1.txt:73: nominal_monthly_hours_kp(2026-01, holidays=empty)        -> 176
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T043/round_01/tests/tests_r1.txt:74: nominal_monthly_hours_kp(2026-01, {2026-01-01,01-06})   -> 160
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T043/round_01/tests/tests_r3.txt:33: `nominal_monthly_hours_kp(month, holidays: frozenset[date] = frozenset())`
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T044/ARCHITECT_FINAL_PREIMPLEMENTATION_GATE.md:50: ceil(layer_hours(k, m) / nominal_monthly_hours_kp(m, POLISH_2026_HOLIDAYS))
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T044/TASK_CHATGPT.md:76: 2. policz potrzebną liczbę osób dla tej warstwy jako `ceil(godziny_warstwy / nominal_monthly_hours_kp(m, POLISH_2026_HOLIDAYS))`;
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T044/TASK_CHATGPT_CORRECTION_R2.md:119: `layer_headcount(k, m) = ceil(layer_hours(k, m) / nominal_monthly_hours_kp(m, POLISH_2026_HOLIDAYS))`
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T044/brief.md:316: / nominal_monthly_hours_kp(m, POLISH_2026_HOLIDAYS))
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T044/round_01/tests/cc_devils_advocate_r1.txt:145: Metoda: bezpośrednie przeliczenie `nominal_monthly_hours_kp` (ten sam kod
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T044/round_01/tests/tests_r3.txt:25: `nominal_monthly_hours_kp(...)` i wzoru zależnego od dni tygodnia
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T044/round_01/tests/tests_r4.txt:16: - REPRO: istniejąca funkcja `nominal_monthly_hours_kp`, święta 2026 i wzór
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T044/round_01/tests/tests_r4.txt:37: python -c "from datetime import date; from math import ceil; from tests.property.coordinator_simulator import nominal_monthly_hours_kp,POLISH_2026_HOLIDAYS,_days_in_month; print([(m, _days_in_month(date(2026,m,1)), nominal_monthly_hours_kp(date(2026,m,1),POLISH_2026_HOLIDAYS), ceil(_days_in_month(date(2026,m,1))*24/(nominal_monthly_hours_kp(date(2026,m,1),POLISH_2026_HOLIDAYS)-24)), ceil(_days_in_month(date(2026,m,1))*48/(nominal_monthly_hours_kp(date(2026,m,1),POLISH_2026_HOLIDAYS)-24))) for m in range(1,13)])"
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:164: def nominal_monthly_hours_kp(month: date, holidays: frozenset[date] = frozenset()) -> int:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:217: target_hours_per_employee = nominal_monthly_hours_kp(month, POLISH_2026_HOLIDAYS)
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:532: target_hours = nominal_monthly_hours_kp(month, POLISH_2026_HOLIDAYS)
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:566: month_target = nominal_monthly_hours_kp(target_month, POLISH_2026_HOLIDAYS)
    TEXT OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:44: # POLISH_2026_HOLIDAYS/nominal_monthly_hours_kp, so every one is a valid
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:87: assert sim.nominal_monthly_hours_kp(date(2026, 1, 1), sim.POLISH_2026_HOLIDAYS) == 160
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:88: assert sim.nominal_monthly_hours_kp(date(2026, 9, 1), sim.POLISH_2026_HOLIDAYS) == 176
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:428: employee_count=5, target_hours_per_employee=sim.nominal_monthly_hours_kp(MONTH, sim.POLISH_2026_HOLIDAYS),
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: random_object_spec
  TOTAL_RAW_HITS: 20
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T038/round_01/tests/tests_r1.txt:30: - niezależny odczyt random_object_spec dla seedów 0..4 na exact SHA:
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T038/round_01/tests/tests_r3.txt:20: a random_object_spec() przekazuje go jako target_hours niezależnie od liczby
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T039/brief.md:20: **Stan po KOREKCIE 1** (patrz sekcja 4a): generator (`random_object_spec`)
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:189: def random_object_spec(seed: int, month: date) -> ObjectSpec:
    TEXT OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:65: # rather than depending on random_object_spec's own seed-to-layer
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:76: layer_counts = {sim.random_object_spec(seed, MONTH).layer_count for seed in range(20)}
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:81: shapes = {sim.random_object_spec(seed, MONTH).shift_shape for seed in range(20)}
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:96: spec = sim.random_object_spec(0, MONTH)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:104: spec = sim.random_object_spec(3, MONTH)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:118: spec = sim.random_object_spec(5, MONTH)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:140: spec = sim.random_object_spec(seed, month)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:168: spec = sim.random_object_spec(0, MONTH)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:180: spec = sim.random_object_spec(0, MONTH)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:190: spec = sim.random_object_spec(0, MONTH)  # 5 LOCAL, D_N_12H (2 posts/day)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:223: spec = sim.random_object_spec(0, MONTH)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:281: spec = sim.random_object_spec(seed, month)
    TEXT OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:420: """random_object_spec's deterministic seed cycle only guarantees
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:467: seed = next(s for s in range(1, 20) if sim.random_object_spec(s, MONTH).shift_shape == "WEEKDAY_12H_WEEKEND_24H")
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:484: spec = sim.random_object_spec(0, MONTH)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:736: spec = sim.random_object_spec(seed, month)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: AbsenceDraw
  TOTAL_RAW_HITS: 10
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:237: class AbsenceDraw:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:266: ) -> list[AbsenceDraw]:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:310: draws.append(AbsenceDraw(
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:400: def apply_absences(client: TestClient, site_id: str, spec: ObjectSpec, draws: list[AbsenceDraw]) -> None:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:727: def count_soft_absence_collisions(assignments: list[dict], absence_draws: list[AbsenceDraw], spec: ObjectSpec) -> int:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:770: analytics_rows: list[dict], absence_draws: list[AbsenceDraw], spec: ObjectSpec,
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:812: def is_symmetric_control_object(spec: ObjectSpec, absence_draws: list[AbsenceDraw]) -> bool:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:821: *, spec: ObjectSpec, absence_draws: list[AbsenceDraw], has_external: bool,
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:183: sim.AbsenceDraw(employee_index=0, kind="SICK_LEAVE", start_date=date(2026, 9, 3), end_date=date(2026, 9, 5)),
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:230: sim.AbsenceDraw(employee_index=0, kind="SICK_LEAVE", start_date=date(2026, 9, 10), end_date=date(2026, 9, 12)),
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: random_absence_set
  TOTAL_RAW_HITS: 11
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T043/round_01/tests/tests_r6.txt:87: - absencje generuje `coordinator_simulator.py::random_absence_set`.
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T043/round_01/tests/tests_r7.txt:42: -k "random_absence_set or plan_and_replan_together"
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:260: def random_absence_set(
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:97: assert sim.random_absence_set(0, spec, spec.employee_count) == []
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:107: draws = sim.random_absence_set(seed, spec, spec.employee_count)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:121: draws = sim.random_absence_set(seed, spec, spec.employee_count)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:141: initial = sim.random_absence_set(seed, spec, spec.employee_count)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:142: replan = sim.random_absence_set(
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:282: absence_draws = sim.random_absence_set(seed, spec, spec.employee_count)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:347: replan_draws = sim.random_absence_set(
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:737: absence_draws = sim.random_absence_set(seed, spec, spec.employee_count)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: local_employee_id
  TOTAL_RAW_HITS: 11
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:317: def local_employee_id(spec: ObjectSpec, index: int) -> str:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:362: employee_id = local_employee_id(spec, index)
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:386: return [local_employee_id(spec, i) for i in range(spec.employee_count)]
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:412: employee_id = local_employee_id(spec, draw.employee_index)
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:569: f"/api/workspace/employees/{local_employee_id(spec, i)}/target-hours",
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:734: (local_employee_id(spec, d.employee_index), d.start_date, d.end_date)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:198: f"/api/workspace/sites/{site_id}/roster/{sim.local_employee_id(spec, i)}", json={"enabled": False},
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:292: "absences": [d.__dict__ | {"employee_id": sim.local_employee_id(spec, d.employee_index)} for d in absence_draws],
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:356: d.__dict__ | {"employee_id": sim.local_employee_id(spec, d.employee_index)}
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:487: resp = client.patch(f"/api/workspace/sites/{site_id}/roster/{sim.local_employee_id(spec, i)}", json={"enabled": False})
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:742: d.__dict__ | {"employee_id": sim.local_employee_id(spec, d.employee_index)}
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: seed_calendar
  TOTAL_RAW_HITS: 18
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T023/round_01/tests/test_t023_checkpoint_c_audit.py:61: seed_calendar=True,
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T043/round_01/tests/tests_r3.txt:35: - tests/property/coordinator_simulator.py:254-256: `seed_calendar()` woła
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:321: def seed_calendar(client: TestClient, site_id: str, month: date) -> None:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:395: seed_calendar(client, site_id, spec.month)
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:577: seed_calendar(client, site_id, month)
    TEXT OTHER_FILE TEST_TASK_ARCH tests/test_t023.py:97: def _leave(conn, *, employee_id: str, kind: AvailabilityKind, start_date: date, end_date: date, active: bool = True, availability_id: str | None = None, site_id: str = SITE, coordinator_id: str = COORDINATOR, seed_calendar: bool = False):
    TEXT OTHER_FILE TEST_TASK_ARCH tests/test_t023.py:98: if seed_calendar:
    TEXT OTHER_FILE TEST_TASK_ARCH tests/test_t023.py:168: record = _leave(conn, employee_id="A", kind=AvailabilityKind.LEAVE_GRANTED, start_date=date(2027, 3, 8), end_date=date(2027, 3, 8), seed_calendar=True)
    TEXT OTHER_FILE TEST_TASK_ARCH tests/test_t023.py:191: record = _leave(conn, employee_id="A", kind=AvailabilityKind.LEAVE_GRANTED, start_date=date(2027, 3, 8), end_date=date(2027, 3, 8), seed_calendar=True)
    TEXT OTHER_FILE TEST_TASK_ARCH tests/test_t023.py:248: leave = _leave(conn, employee_id="A", kind=AvailabilityKind.LEAVE_GRANTED, start_date=date(2027, 3, 8), end_date=date(2027, 3, 8), availability_id="AV-A-LEAVE", seed_calendar=True)
    TEXT OTHER_FILE TEST_TASK_ARCH tests/test_t023.py:262: record = _leave(conn, employee_id="A", kind=AvailabilityKind.LEAVE_GRANTED, start_date=date(2027, 3, 8), end_date=date(2027, 3, 8), seed_calendar=True)
    TEXT OTHER_FILE TEST_TASK_ARCH tests/test_t023.py:304: before_record = _leave(conn, employee_id="A", kind=AvailabilityKind.LEAVE_GRANTED, start_date=date(2027, 4, 1), end_date=date(2027, 4, 1), availability_id="AV-A-1", seed_calendar=True)
    TEXT OTHER_FILE TEST_TASK_ARCH tests/test_t023.py:318: record = _leave(conn, employee_id="A", kind=AvailabilityKind.LEAVE_GRANTED, start_date=date(2027, 3, 8), end_date=date(2027, 3, 12), seed_calendar=True)
    TEXT OTHER_FILE TEST_TASK_ARCH tests/test_t023.py:327: record = _leave(conn, employee_id="A", kind=AvailabilityKind.LEAVE_GRANTED, start_date=date(2027, 3, 8), end_date=date(2027, 3, 8), seed_calendar=True)
    TEXT OTHER_FILE TEST_TASK_ARCH tests/test_t023.py:342: record = _leave(conn, employee_id="A", kind=AvailabilityKind.SICK_LEAVE, start_date=date(2027, 3, 8), end_date=date(2027, 3, 8), seed_calendar=True)
    TEXT OTHER_FILE TEST_TASK_ARCH tests/test_t023.py:391: record = _leave(conn, employee_id="A", kind=AvailabilityKind.SICK_LEAVE, start_date=date(2027, 3, 31), end_date=date(2027, 4, 1), seed_calendar=True)
    TEXT OTHER_FILE TEST_TASK_ARCH tests/test_t023.py:443: leave = _leave(conn, employee_id="A", kind=AvailabilityKind.LEAVE_GRANTED, start_date=date(2027, 3, 8), end_date=date(2027, 3, 8), availability_id="AV-A-L", seed_calendar=True)
    TEXT OTHER_FILE TEST_TASK_ARCH tests/test_t023_checkpoint_c.py:130: _leave(conn, employee_id="A", kind=AvailabilityKind.LEAVE_GRANTED, start_date=date(2027, 3, 8), end_date=date(2027, 3, 8), site_id="SITE-A", seed_calendar=True)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: _create_site
  TOTAL_RAW_HITS: 2
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:334: def _create_site(client: TestClient, spec: ObjectSpec) -> str:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:393: site_id = _create_site(client, spec)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: _put_shift_catalog
  TOTAL_RAW_HITS: 3
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:91: # _put_shift_catalog multiplies it by ObjectSpec.layer_count for a second
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:346: def _put_shift_catalog(client: TestClient, site_id: str, spec: ObjectSpec) -> None:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:394: _put_shift_catalog(client, site_id, spec)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: add_local_employee
  TOTAL_RAW_HITS: 2
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:358: def add_local_employee(client: TestClient, site_id: str, spec: ObjectSpec, index: int, day_only: bool) -> str:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:380: add_local_employee(client, site_id, spec, i, day_only=i in spec.day_only_indices)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: _add_local_roster
  TOTAL_RAW_HITS: 2
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:378: def _add_local_roster(client: TestClient, site_id: str, spec: ObjectSpec) -> None:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:396: _add_local_roster(client, site_id, spec)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: declared_roster
  TOTAL_RAW_HITS: 6
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:383: def declared_roster(spec: ObjectSpec) -> list[str]:
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:170: assert len(sim.declared_roster(spec)) == 5
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:176: assert set(used) <= set(sim.declared_roster(spec))
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:283: employees = sim.declared_roster(spec)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:432: employees = sim.declared_roster(spec)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:497: employees = sim.declared_roster(spec)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: build_object
  TOTAL_RAW_HITS: 15
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T038/round_01/tests/tests_r1.txt:50: - Niezależny pion build_object(TestClient, SQLite :memory:) dla seed=2,
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T038/round_01/tests/tests_r1.txt:70: build_object(), przed _run_plan();
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T038/round_01/tests/tests_r1.txt:71: - niezależny GET roster po build_object(seed=2), nadal przed PLAN-em, zwrócił
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T038/round_01/tests/tests_r1.txt:91: :memory:, bez solvera: ObjectSpec -> build_object -> roster/target readback.
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T038/round_01/tests/tests_r2.txt:49: employee_count=5 i target_hours_per_employee=144. Po build_object() odczyt
    CALL OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T038/round_01/tests/tests_r3.txt:38: Mechanizm spełnia zaakceptowaną granicę: początkowy build_object() tworzy tylko
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:389: def build_object(client: TestClient, spec: ObjectSpec) -> str:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:560: site_id = build_object(client, spec)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:169: site_id = sim.build_object(client, spec)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:181: site_id = sim.build_object(client, spec)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:191: site_id = sim.build_object(client, spec)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:224: site_id = sim.build_object(client, spec)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:285: site_id = sim.build_object(client, spec)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:438: site_id = sim.build_object(client, spec)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:485: site_id = sim.build_object(client, spec)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: apply_absences
  TOTAL_RAW_HITS: 6
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:400: def apply_absences(client: TestClient, site_id: str, spec: ObjectSpec, draws: list[AbsenceDraw]) -> None:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:405: and start_date) so two SEPARATE apply_absences() calls on the same site
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:182: sim.apply_absences(client, site_id, spec, [
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:229: sim.apply_absences(client, site_id, spec, [
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:286: sim.apply_absences(client, site_id, spec, absence_draws)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:360: sim.apply_absences(client, site_id, spec, replan_draws)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: external_support_employee_id
  TOTAL_RAW_HITS: 3
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:424: def external_support_employee_id(spec: ObjectSpec) -> str:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:451: employee_id = external_support_employee_id(spec)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:207: assert reaction["external_employee_id"] == sim.external_support_employee_id(spec)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: external_support_reaction
  TOTAL_RAW_HITS: 8
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:31: DECISION_REQUIRED -- see external_support_reaction() -- never a
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:428: def external_support_reaction(client: TestClient, site_id: str, month: date, spec: ObjectSpec) -> dict:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:448: assert decision is not None, "external_support_reaction called with no persisted DECISION_REQUIRED"
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:548: DECISION_REQUIRED via external_support_reaction() if needed (without
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:588: external = external_support_reaction(client, site_id, month, spec)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:206: reaction = sim.external_support_reaction(client, site_id, MONTH, spec)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:304: external = sim.external_support_reaction(client, site_id, month, spec)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:491: external = sim.external_support_reaction(client, site_id, MONTH, spec)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: run_plan
  TOTAL_RAW_HITS: 10
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:479: second_result = run_plan(client, site_id, month)
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:483: def run_plan(client: TestClient, site_id: str, month: date) -> dict:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:579: first_result = run_plan(client, site_id, month)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:172: result = sim.run_plan(client, site_id, MONTH)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:185: result = sim.run_plan(client, site_id, MONTH)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:202: first_result = sim.run_plan(client, site_id, MONTH)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:225: first = sim.run_plan(client, site_id, MONTH)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:288: first_result = sim.run_plan(client, site_id, month)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:439: first_result = sim.run_plan(client, site_id, MONTH)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:489: first_result = sim.run_plan(client, site_id, MONTH)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: run_replan
  TOTAL_RAW_HITS: 3
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:489: def run_replan(client: TestClient, site_id: str, month: date) -> dict:
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:232: replanned = sim.run_replan(client, site_id, MONTH)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:361: replanned = sim.run_replan(client, site_id, month)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: select_first_candidate
  TOTAL_RAW_HITS: 3
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:495: def select_first_candidate(client: TestClient, site_id: str, month: date, result: dict) -> None:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:598: select_first_candidate(client, site_id, month, base_for_candidate)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:227: sim.select_first_candidate(client, site_id, MONTH, first)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: get_analytics
  TOTAL_RAW_HITS: 8
  WARNING: name defined in 2 different files — hits below may belong to a different symbol of the same name:
    DEF SITE: api/routers/analytics.py
    DEF SITE: tests/property/coordinator_simulator.py
    DEF  OTHER_FILE PRODUCTION     api/routers/analytics.py:58: def get_analytics(site_id: str, month: str, conn=Depends(get_conn)) -> CoordinatorAnalyticsViewOut:
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:506: def get_analytics(client: TestClient, site_id: str, month: date) -> dict:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:514: -- a different, genuinely independent data path from get_analytics()'s
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:599: analytics = get_analytics(client, site_id, month)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:324: analytics = sim.get_analytics(client, site_id, month)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:378: r_analytics = sim.get_analytics(client, site_id, month)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:445: analytics = sim.get_analytics(client, site_id, MONTH)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:496: analytics = sim.get_analytics(client, site_id, MONTH)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: get_month_view
  TOTAL_RAW_HITS: 2
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:512: def get_month_view(client: TestClient, site_id: str, month: date) -> dict:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:600: month_view = get_month_view(client, site_id, month)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: quarter_object_spec
  TOTAL_RAW_HITS: 2
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:528: def quarter_object_spec(seed: int, month: date) -> ObjectSpec:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:559: spec = quarter_object_spec(seed, QUARTER_MONTHS[0])
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: run_quarter
  TOTAL_RAW_HITS: 6
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T043/round_01/tests/coordinator_report.md:195: | | `tests/property/test_coordinator_simulator.py`'s `run_quarter`/B6 oracle (T043) | REAL_API |
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:541: def run_quarter(client: TestClient, seed: int) -> dict:
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:241: result = sim.run_quarter(client, seed=0)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:517: quarter_result = sim.run_quarter(client, seed=0)
    TEXT OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:619: | | `tests/property/test_coordinator_simulator.py`'s `run_quarter`/B6 oracle (T043) | REAL_API |
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:756: quarter_result = sim.run_quarter(client, seed=0)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: _duration_hours
  TOTAL_RAW_HITS: 4
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:622: def _duration_hours(a: dict) -> float:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:646: hours[a["employee_id"]] += _duration_hours(a)
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:656: hours[a["employee_id"]] += _duration_hours(a)
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:666: hours[a["employee_id"]] += _duration_hours(a)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: _is_active_primary
  TOTAL_RAW_HITS: 10
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:628: def _is_active_primary(a: dict) -> bool:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:645: if _is_active_primary(a) and a["employee_id"] in hours:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:653: if not _is_active_primary(a) or a["employee_id"] not in hours:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:663: if not _is_active_primary(a) or a["employee_id"] not in hours:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:677: if not _is_active_primary(a):
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:741: if not _is_active_primary(a):
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:329: primary_atom_count = sum(1 for a in view["assignments"] if sim._is_active_primary(a))
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:383: r_primary_atoms = sum(1 for a in r_view["assignments"] if sim._is_active_primary(a))
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:450: primary_atoms = sum(1 for a in view["assignments"] if sim._is_active_primary(a))
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:504: primary_atom_count=sum(1 for a in view["assignments"] if sim._is_active_primary(a)),
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: demand_kind_by_id
  TOTAL_RAW_HITS: 2
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:632: def demand_kind_by_id(demands: list[dict]) -> dict[str, str | None]:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:775: kind_by_demand = demand_kind_by_id(demands)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: actual_hours_by_employee
  TOTAL_RAW_HITS: 2
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:642: def actual_hours_by_employee(assignments: list[dict], employees: list[str]) -> dict[str, float]:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:777: actual = actual_hours_by_employee(assignments, employees)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: weekend_hours_by_employee
  TOTAL_RAW_HITS: 2
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:650: def weekend_hours_by_employee(assignments: list[dict], employees: list[str]) -> dict[str, float]:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:778: weekend = weekend_hours_by_employee(assignments, employees)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: holiday_hours_by_employee
  TOTAL_RAW_HITS: 2
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:660: def holiday_hours_by_employee(assignments: list[dict], employees: list[str], holidays: frozenset[date]) -> dict[str, float]:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:779: holiday = holiday_hours_by_employee(assignments, employees, POLISH_2026_HOLIDAYS)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: _employee_day_kinds
  TOTAL_RAW_HITS: 2
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:670: def _employee_day_kinds(assignments: list[dict], kind_by_demand: dict[str, str | None]) -> dict[tuple[str, date], str]:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:776: day_kind = _employee_day_kinds(assignments, kind_by_demand)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: count_dn_rhythm_windows
  TOTAL_RAW_HITS: 2
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:690: def count_dn_rhythm_windows(day_kind: dict[tuple[str, date], str], employees: list[str], month: date) -> dict[str, int]:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:806: dn_rhythm_windows=count_dn_rhythm_windows(day_kind, employees, spec.month),
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: count_bad_third_windows
  TOTAL_RAW_HITS: 2
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:710: def count_bad_third_windows(day_kind: dict[tuple[str, date], str], employees: list[str], month: date) -> dict[str, int]:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:807: bad_third_windows=count_bad_third_windows(day_kind, employees, spec.month),
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: count_soft_absence_collisions
  TOTAL_RAW_HITS: 2
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:727: def count_soft_absence_collisions(assignments: list[dict], absence_draws: list[AbsenceDraw], spec: ObjectSpec) -> int:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:808: soft_absence_collisions=count_soft_absence_collisions(assignments, absence_draws, spec),
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: FairnessFacts
  TOTAL_RAW_HITS: 3
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:752: class FairnessFacts:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:771: ) -> FairnessFacts:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:801: return FairnessFacts(
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: compute_fairness_facts
  TOTAL_RAW_HITS: 6
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:768: def compute_fairness_facts(
    TEXT OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:11: (coordinator_simulator.py's compute_fairness_facts/evaluate_fairness/
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:325: facts = sim.compute_fairness_facts(
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:379: r_facts = sim.compute_fairness_facts(
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:446: facts = sim.compute_fairness_facts(
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:498: facts = sim.compute_fairness_facts(
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: is_symmetric_control_object
  TOTAL_RAW_HITS: 3
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:812: def is_symmetric_control_object(spec: ObjectSpec, absence_draws: list[AbsenceDraw]) -> bool:
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:829: if has_external or not is_symmetric_control_object(spec, absence_draws):
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:431: assert sim.is_symmetric_control_object(spec, [])
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: evaluate_fairness
  TOTAL_RAW_HITS: 7
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T044/CODEX_OWNER_CLARIFICATION_R1.md:45: Dlatego usuń z T044 testowy `evaluate_fairness` jako sędziego Wariantu B.
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:820: def evaluate_fairness(
    TEXT OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:11: (coordinator_simulator.py's compute_fairness_facts/evaluate_fairness/
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:330: fairness_verdict, fairness_detail = sim.evaluate_fairness(
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:384: r_verdict, r_detail = sim.evaluate_fairness(
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:451: verdict, _ = sim.evaluate_fairness(
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:502: verdict, _ = sim.evaluate_fairness(
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: select_first_candidate_reporting_validate
  TOTAL_RAW_HITS: 6
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:842: def select_first_candidate_reporting_validate(client: TestClient, site_id: str, month: date, result: dict) -> tuple[str, str | None, list[dict]]:
    TEXT OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:12: select_first_candidate_reporting_validate/compute_quarter_oracle), the
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:316: validate_status, validate_detail, candidate = sim.select_first_candidate_reporting_validate(client, site_id, month, base_result)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:376: r_validate_status, r_validate_detail, r_candidate = sim.select_first_candidate_reporting_validate(client, site_id, month, replanned)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:442: validate_status, _, _ = sim.select_first_candidate_reporting_validate(client, site_id, MONTH, first_result)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:494: _, _, _ = sim.select_first_candidate_reporting_validate(client, site_id, MONTH, external["second_plan_result"])
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: _actual_primary_hours_by_employee
  TOTAL_RAW_HITS: 3
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:857: def _actual_primary_hours_by_employee(assignments: list[dict], month: date) -> dict[str, int]:
    TEXT SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:882: _actual_primary_hours_by_employee -- NOT read from analytics'
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:905: actual_by_employee = _actual_primary_hours_by_employee(month_entry["assignments"], month)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: compute_quarter_oracle
  TOTAL_RAW_HITS: 9
    TEXT OTHER_FILE TEST_TASK_ARCH tasks/ROTA-T043/round_01/tests/tests_r6.txt:135: `coordinator_simulator.py::compute_quarter_oracle`.
    CALL SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:516: compute_quarter_oracle() to sum actual PRIMARY hours itself rather than
    DEF  SAME_FILE  TEST_TASK_ARCH tests/property/coordinator_simulator.py:878: def compute_quarter_oracle(quarter_result: dict) -> dict:
    TEXT OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:12: select_first_candidate_reporting_validate/compute_quarter_oracle), the
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:521: oracle = sim.compute_quarter_oracle(quarter_result)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:535: stripped_oracle = sim.compute_quarter_oracle(stripped)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:557: corrupted_oracle = sim.compute_quarter_oracle(corrupted)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:570: missing_oracle = sim.compute_quarter_oracle(missing)
    CALL OTHER_FILE TEST_TASK_ARCH tests/property/test_coordinator_simulator.py:760: quarter_oracle = sim.compute_quarter_oracle(quarter_result)
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: DemandAtomB
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: _weekday_occurrences_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: layer_hours_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: layered_headcount_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: has_any_coverage_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: _dn_12h_atoms_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: ObjectSpecB
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: random_atoms_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: random_object_spec_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: catalog_rows_from_atoms_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: local_employee_id_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: declared_roster_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: _create_site_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: _put_shift_catalog_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: add_local_employee_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: build_object_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: _workday_count_in_range
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: _next_monday_on_or_after
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: urlop_blocks_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: sick_leave_draw_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: initial_absences_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: apply_absences_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: external_support_employee_id_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: external_support_reaction_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: run_with_external_loop_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: assert_headcount_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

SYMBOL: assert_closed_world_b
  TOTAL_RAW_HITS: 0
  NOTE: raw text-match evidence only — no reachability/ownership verdict is computed. Read each flagged line yourself, especially DEF hits (possible duplicate name) and CALL hits in files other than the one you're analyzing.

