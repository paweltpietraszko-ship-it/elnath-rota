# ROTA-T020 — CHECKPOINT B ARCHITECT PREIMPLEMENTATION ACCEPTANCE

STATUS: PASS — READY FOR CC IMPLEMENTATION
DATE: 2026-08-20
TASK_ID: ROTA-T020
TASK_BRANCH: arch/rota-t020-schedule-export-2026-08-20
ACCEPTANCE_PARENT_HEAD: 7006171d2e2737a32a73de5c513852bbcc687dae
FINAL_AUDITED_CONTRACT_HEAD: 71383665d06aed02ce32e452b7a9f85ad9c9559b
FINAL_AUDIT_COMMIT: 1e02f66e00a48cf7b95de41b6d920900e83d036c
FINAL_AUDIT_REPORT: tasks/ROTA-T020/round_01/tests/tests_r7.txt

## 1. ARCHITECT VERDICT

ARCHITECT PREIMPLEMENTATION GATE ACCEPTANCE — ROTA-T020 CHECKPOINT B: PASS — READY FOR CC IMPLEMENTATION.

I accept the frozen Checkpoint B implementation contract after the independent Codex audit sequence and the final Round 7 verdict:

`PASS — REMAINING R5 FINDINGS CLOSED — READY_FOR_IMPLEMENTATION`

No unresolved product or architecture finding remains inside the authorized Checkpoint B scope.

This acceptance authorizes implementation only. It is not final product acceptance and it is not merge authorization.

## 2. EXACT EVIDENCE ACCEPTED

The final independent Round 7 audit inspected exact contract HEAD:

`71383665d06aed02ce32e452b7a9f85ad9c9559b`

and was committed as:

`1e02f66e00a48cf7b95de41b6d920900e83d036c`.

Round 7 confirms:

- remaining R5-1 closed;
- normal catalog-H24 cross-month/cross-year semantics closed;
- emergency T012 cross-month/cross-year semantics closed;
- corresponding R5-4 test matrix closed, including corrected T20-31 and T20-38..T20-41;
- exact SHA / no production change sanity PASS;
- committed T012 cross-boundary mechanical tests 5/5 PASS;
- no direct contradiction with R5-2 or earlier PASS sections.

Round 5 previously established 845/845 PASS on the same unchanged product/test tree. Subsequent contract/audit commits through Round 7 changed no production code or production tests.

## 3. FROZEN IMPLEMENTATION CONTRACT

CC must implement the contract hierarchy stated in `tasks/ROTA-T020/brief.md`.

In particular:

- Checkpoint A accepted visual artifacts remain frozen;
- `CHECKPOINT_B_OWNER_DECISIONS_05.md` is authoritative for absence presentation;
- `CHECKPOINT_B_R6_AMENDMENT.md` supersedes conflicting R5 details;
- remaining R5 amendment details stay binding where not superseded;
- `CHECKPOINT_B_CONTRACT.md` supplies the remaining technical contract.

No implementation-time reinterpretation of owner behavior is authorized.

## 4. SOLVER / U-C BOUNDARY REMAINS FROZEN

T020 must not change operational solver coverage.

U/C presentation:

- is generated by the export/read path only;
- does not create Assignment;
- does not cover ShiftDemand;
- does not reduce real Site demand;
- does not change WorkBalance, solver eligibility, ScheduleVersion lifecycle or T018 accounting.

The frozen 40h presentation example remains:

- PLAN `D1 / D1 / N2` = 40h;
- WYK `U1 / U1 / U2` = 40h.

## 5. AUTHORIZED IMPLEMENTATION SCOPE

Canonical `TASK_SCOPE` remains exactly:

- `pyproject.toml`
- `rota/application/schedule_export.py`
- `rota/persistence/db.py`
- `rota/persistence/site_repository.py`
- `rota/persistence/employee_repository.py`
- `tests/test_t020.py`

Exactly two new non-pipeline implementation files are authorized:

- `rota/application/schedule_export.py`;
- `tests/test_t020.py`.

All other listed paths are modifications to existing files only.

## 6. EXPLICITLY FORBIDDEN PATHS / REDESIGN

CC must not change:

- `rota/planning/**`;
- `rota/balance.py`;
- `rota/domain.py`;
- `rota/persistence/schedule_lifecycle.py`;
- `arch/spec.md`;
- `arch/FROZEN.lock`;
- `Grafiki/**`;
- accepted Checkpoint A PDFs/renderer.

No second solver, second Assignment layer, export-history table, generic workflow/event system, generic settings subsystem, second schedule model or PDF-owned planning truth is authorized.

If implementation proves a forbidden-path change is necessary, CC stops and reports the exact blocker to architect. It does not widen scope itself.

## 7. IMPLEMENTATION BEFORE_SHA

The canonical backend implementation `before_sha` is the exact commit that contains this acceptance record — i.e. the branch HEAD immediately after this file is committed.

CC must capture that exact SHA before its first production implementation commit and use it as the implementation backend/diff base.

Do not use `main`, `71383665...`, or `1e02f66...` as the implementation diff base once this acceptance commit exists.

## 8. IMPLEMENTATION GATE

CC MAY START ROTA-T020 CHECKPOINT B IMPLEMENTATION.

After implementation, independent implementation audit is still required against the exact product-code SHA and this frozen contract before any architect final acceptance.

No merge to `main` is authorized by this document.
