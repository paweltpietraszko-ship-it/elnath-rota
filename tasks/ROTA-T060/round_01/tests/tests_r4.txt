ROTA-T060 — CODE AUDIT R4

VERDICT: FAIL
EXACT AUDITED SHA: 1354fa6
BASE / FROZEN BRIEF: f5f76cd712697150302f7a64b199e2d8caa38825

R4-01 — Historia nadal pokazuje techniczne identyfikatory Assignmentu

TRACE

- OWNER contract / brief 2.1, 3.6, T60-05 i T60-06: Historia nie może
  renderować technicznych ID, także zagnieżdżonych w before_state/after_state.
- T060 nie może ograniczyć ochrony do kilku przykładowych nazw kluczy.

OWNERSHIP

- `rota/application/manual_edit.py::_assignment_state` i
  `rota/application/plan_ops.py::_assignment_fact` zapisują do rzeczywistej
  Historii: `schedule_version_id`, `assignment_id`, `covers_demand_id`,
  `mentor_primary_assignment_id`, `work_period_id`; wrappery dodają również
  `parent_version_id`/`child_version_id`.
- `frontend/src/screens/History.tsx::renderStateValue` jest ownerem prezentacji
  tego generycznego DTO. Nowe `ID_ONLY_STATE_KEYS` nie zawiera żadnego z tych
  kluczy, więc funkcja zwraca surowy string.
- Analogicznie lifecycle finalization zapisuje zagnieżdżone `deviation_id`,
  `source_reference`, `affected_assignment_or_employee` i `acknowledged_by`,
  których lista także nie chroni.

REPRO NA EXACT SHA

Niezależny test Chromium uruchomił prawdziwy komponent Historia i przekazał
mu dokładny kształt DTO produkowany przez realny manual-edit writer. Po
rozwinięciu „Szczegóły” DOM pokazał m.in.:

- `parent_version_id: SV-PARENT-SECRET`
- `schedule_version_id: SV-ASSIGNMENT-SECRET`
- `assignment_id: ASSIGNMENT-SECRET`
- `covers_demand_id: DEMAND-SECRET`
- `mentor_primary_assignment_id: MENTOR-SECRET`
- `work_period_id: WORK-PERIOD-SECRET`

Command:
`npm exec playwright test -- --config playwright.audit-t060.config.ts --timeout 15000`

Result: reproducer PASS (1/1): wszystkie trzy jawne asercje wycieku były
widoczne w realnym DOM. Evidence:
`frontend/e2e/t060-audit-r4.spec.ts` + `frontend/playwright.audit-t060.config.ts`.

SIBLING / TEST GAP

Implementacja nie dodała zapowiedzianych w briefie frontendowych testów dla
Overview/Decisions/History/Export/client; exact diff zawiera tylko Python
`tests/test_t060.py`. To wyjaśnia, dlaczego testy implementera nie obejmują
rzeczywistego generycznego renderera Historii. Nie jest to osobny finding —
naprawa R4-01 ma dodać wąski test rzeczywistych kształtów manual correction,
candidate delta i Deviation, zamiast listy kilku ręcznie wybranych ID.

POZOSTAŁE WĄSKIE GATE'Y

- `python -m pytest -q tests/test_t060.py`: 11 passed.
- `npm run build`: PASS (`tsc -b` + Vite, 53 modules).
- `git diff --check f5f76cd..1354fa6`: PASS.
- TASK_SCOPE: wszystkie zmienione pliki należą do zamrożonego scope.

FAIL jest wąski. Backend public-error marker, roster bypass, client fallback,
DAY_SHIFT_OFF friendly label, neutralny koordynator i granice Export/PDF nie
zostały podważone przez ten reproduktor. Po poprawce wystarczy re-check
History renderer dla rzeczywistych zapisanych kształtów oraz istniejące testy
T060/build; bez pełnej regresji repo.
