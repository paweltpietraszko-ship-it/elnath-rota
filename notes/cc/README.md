# CC scratchpad branch

Owner: CC only. Never merged to `main`, never read as a source of truth by
backend.py/Codex/architect. Purpose: cheap, git-durable continuity for
things that would otherwise live only in conversation turns and be at risk
across compaction — not a replacement for briefs/commits/CLAUDE.md, just a
faster place to park an in-progress thought before it's ready to become one
of those.

Pushed freely, without asking each time (owner's call, 2026-08-28).

**One file per task/topic** (owner's rule, 2026-08-28): never one growing
log — that's the Master.md-2500-lines mistake again. A new finding about
T040 goes in `T040.md`, not appended to whatever file I happened to touch
last. Each topic file stays short and current-state oriented; when a topic
closes (merged, decided, dropped), trim it to a short summary instead of
leaving a play-by-play. This index lists one line per file — keep entries
here short enough to actually scan.

## Index

- `T040.md` — H24 false-infeasibility fix (fairness.py), merged to main 2ccfcc8
- `AUDIT-1.md` — Codex-run audit, layers A-D; placeholder until it starts
