# T040 — H24 false-infeasibility fix (fairness.py D/N rhythm reward)

Status: **MERGED to main, `2ccfcc8`** (2026-08-28). Fix scoped to
`fairness.py` only (`_occupied_bool`), see the merge commit and
`tasks/ROTA-T040/brief.md` for the full architect-authored root cause.

## Post-merge follow-ups worth remembering

- Full `pytest tests/` after merge: **1235 passed, 3 failed**. Confirmed via
  a throwaway `d445ee6` worktree that all 3 failures are **pre-existing**,
  unrelated to T040 (T040 touches only `fairness.py` + tests/docs):
  - `test_sick_leave.py::test_leave_granted_does_not_reduce_solver_target_unlike_sick_leave`
  - `test_t017.py::test_m13_first_candidate_matches_pre_t017_deterministic_result`
  - `test_t023_checkpoint_b.py::test_t23_54_t012_legality_rest_modules_unmodified_by_checkpoint_b`
    — this one asserts `constraints.py`/`work_periods.py` are byte-unmodified
    since some `BASE_SHA`; that SHA is now stale relative to `main` (some
    later merged task legitimately touched those files after this test's
    baseline was set). Someone needs to bump `BASE_SHA` in that test or
    confirm the touching task was in fact out-of-scope. Not investigated
    further — flagged, not fixed, per role.
- Side-effect spotted by accident (not asserted by any test): running the
  full suite regenerates `tasks/ROTA-T038/round_01/tests/simulator_report.md`
  as a byproduct of `test_coordinator_simulator.py`. On this run, seed 2
  (SINGLE_24H/OCHRONA) flipped from **DECISION_REQUIRED** (false
  "collides with 2-consecutive-night limit") pre-T040 to **FEASIBLE**
  post-T040, same 5 employees. Good incidental confirmation the fix has
  real effect beyond the synthetic reproducer — discarded the working-tree
  change on `main` (`git checkout --`) rather than committing it, since it
  wasn't an intentional task.

## Open thread this surfaced (separate topic, own brief filed)

While manually testing T040 live against Paweł's Test1 object
(`SITE-af2c7186283e4a4cbc3fb84b99fa1792`), found and root-caused a
different, unrelated defect: an employee missing a `work_balance_targets`
row for the planned month becomes invisible to the *entire* fairness
objective (not just their own target — `add_target_equity_fairness` and
TARGET-01 both skip them silently), producing a proven-OPTIMAL but grossly
unequal schedule (4×168h + 1×48h) with zero warning surfaced to the
coordinator. Written up separately:
`arch/ARCHITECT_BRIEF_EMPLOYEE_OMITTED_FROM_FAIRNESS_OBJECTIVE_2026-08-28.md`,
branch `docs/worker-omitted-from-fairness-objective`, commit `e4c73a5`. Not
merged, not actioned — architect's call.
